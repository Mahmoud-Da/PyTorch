*** 134. What Is a Custom Dataset and What We Are Going to Cover ***
## **Lecture Notes: Custom Datasets in PyTorch**

### **1. Introduction to Custom Datasets**

* So far, various PyTorch models have been built using standard datasets (e.g., FashionMNIST).
* The question arises: *Can PyTorch models be built using your own custom data?*
* **Answer:** Yes, but the data must go through preprocessing steps to make it compatible with PyTorch.

---

### **2. PyTorch Domain Libraries**

PyTorch offers specialized libraries for different types of data:

* **torchvision**: For image (vision) data (e.g., classifying photos of food).
* **torchtext**: For natural language/text data (e.g., sentiment analysis).
* **torchaudio**: For audio data (e.g., identifying music like Shazam).
* **torchrec**: For recommendation systems (e.g., Netflix home page).
* **torchdata** (in beta as of April 2022): General data loading utilities.

Each domain library contains:

* Prebuilt datasets.
* Functions to load and preprocess your own custom data.

---

### **3. Example Use Case: Building a Custom Dataset**

#### Goal:

Build an image classification model named **FoodVision Mini**.

#### Dataset:

* Custom image dataset including: **pizza, sushi, and steak**.
* Based on the **Food101** dataset.

#### Objective:

* Load images into PyTorch.
* Train a model to classify images as either pizza, sushi, or steak.
* Use this model in a practical app (e.g., **NutriFact App**).

---

### **4. The PyTorch Workflow**

To work with custom data, we will follow a familiar PyTorch workflow:

1. **Load the custom dataset** using prebuilt or custom functions.
2. **Transform and visualize** the data.
3. **Prepare the data** for modeling (e.g., using transformations).
4. **Build and train** the model:

   * Define model architecture.
   * Choose loss function and optimizer.
   * Write a training loop.
   * Evaluate performance.
5. **Improve** the model through experimentation (e.g., data augmentation).
6. **Make predictions** on new, unseen custom data.
7. **Save and reload** the trained model.

---

### **5. Summary of What Will Be Learned**

* How to work with **custom datasets** in PyTorch.
* How to use domain libraries effectively (vision, text, audio, recommendation).
* How to load and transform your own data for training.
* How to **build, train, evaluate, and test** a computer vision model.
* Importance of **predicting on your own data** to validate real-world performance.
* Intro to **data augmentation** to boost model performance.

---

### **Conclusion**

Creating a machine learning model is not just science—it's also art. This section emphasizes hands-on, creative development, treating code like a recipe in a kitchen—experiment, build, and iterate.

---

*** 135. Importing PyTorch and Setting Up Device Agnostic Code ***
## **Lecture Notes: Setting Up PyTorch for Custom Datasets**

### **1. Objective**

This lecture introduces the initial setup for working with **custom datasets in PyTorch** using Google Colab. The focus is on preparing the environment, understanding the purpose of custom datasets, and reinforcing the use of PyTorch domain libraries.

---

### **2. Google Colab Setup**

* Navigate to: `https://colab.research.google.com`
* Click **“New Notebook”**
* Rename the notebook to something meaningful, e.g.,
  `04_pytorch_custom_datasets_video.ipynb`
* This notebook will contain all the code from the video and is placed in the **video notebooks** folder of the PyTorch Deep Learning repo.

---

### **3. Notebook Structure**

Add initial markdown cells:

* **Title**: `PyTorch Custom Datasets – Video Notebook`
* **Resources**:

  * Book version of the course materials
  * Ground-truth version of Notebook 04 (for reference and validation)

---

### **4. Purpose of This Section**

* Previous lessons used built-in datasets in PyTorch.
* The goal now is to learn **how to load your own data** into PyTorch.
* Custom datasets allow you to tackle **real-world problems with your own data**.

---

### **5. Domain Libraries Reminder**

Reiterate the use of **PyTorch domain libraries** depending on the data type:

* `torchvision` for images (vision)
* `torchtext` for text
* `torchaudio` for audio
* `torchrec` for recommendation systems

Each library has dataset modules and helper functions for loading various types of data.

---

### **6. Initial Code Setup**

#### a. **Importing Required Libraries**

-------------------code----------------------
import torch
-------------------code----------------------

#### b. **Importing Neural Network Module**

-------------------code----------------------
from torch import nn
-------------------code----------------------

#### c. **Checking PyTorch Version**

-------------------code----------------------
print(torch.__version__)
-------------------code----------------------

* Ensure PyTorch **v1.10.0 or higher** is installed (minimum requirement for the course).

#### d. **Device Agnostic Code**

Set up the device to use GPU if available:

-------------------code----------------------
device = "cuda" if torch.cuda.is_available() else "cpu"
print(f"Using device: {device}")
-------------------code----------------------

---

### **7. GPU Setup in Google Colab**

By default, new Colab instances use **CPU**. To enable **GPU**:

* Click `Runtime` → `Change runtime type`
* Select `GPU` as the **hardware accelerator**
* Re-run the code to verify CUDA is available

#### Example:

-------------------code----------------------
!nvidia-smi
-------------------code----------------------

* Output shows GPU details (e.g., **Tesla P100** with 16GB memory in this case)

> Note: Colab Pro users may have access to faster and more reliable GPUs, but it is **not required** for completing the course.

---

### **8. Summary of This Lecture**

* Introduced the concept and motivation behind using **custom datasets** in PyTorch.
* Set up a new Colab notebook environment.
* Reviewed **domain libraries** in PyTorch.
* Ensured PyTorch version compatibility and **device configuration** for optimized training.

---

### **Next Steps**

In the following sections, we will:

* Work with a real custom dataset
* Load and preprocess our own image data
* Build a computer vision model using this dataset

*** 136. Downloading a Custom Dataset of Pizza, Steak and Sushi Images ***
## **PyTorch Custom Datasets – Part 2: Obtaining the Dataset**

### **Objective**

Prepare a smaller custom dataset (Pizza, Steak, Sushi) derived from the **Food101 dataset** to practice working with custom data in PyTorch.

---

### **1. Overview:**

* The full **Food101** dataset contains:

  * **101 food categories**
  * **101,000 images**
  * Each class has:

    * **750 training images**
    * **250 test images**
* For this course, a **smaller subset** was created:

  * **3 categories**: `pizza`, `steak`, `sushi`
  * **10% of original data**: \~75 train / 25 test images per class

**Reason for Subsetting**:
Smaller datasets allow **faster experimentation** and **faster model training**. Always begin with a small version of the data and scale up as needed.

---

### **2. Dataset Structure**

Created in **image classification style**:

-------------------code----------------------
pizza_steak_sushi/
│
├── train/
│   ├── pizza/
│   ├── steak/
│   └── sushi/
│
└── test/
    ├── pizza/
    ├── steak/
    └── sushi/
-------------------code----------------------

Creation process documented in the `extras` folder:
**Extras → Custom Data Creation → pizza\_steak\_sushi notebook**

---

### **3. Downloading the Dataset**

#### **Libraries Used**

-------------------code----------------------
import requests
import zipfile
from pathlib import Path
-------------------code----------------------

#### **Set Paths**

-------------------code----------------------
data_path = Path("data")
image_path = data_path / "pizza_steak_sushi"
-------------------code----------------------

#### **Check for Existing Data**

-------------------code----------------------
if image_path.is_dir():
    print(f"[INFO] {image_path} directory exists. Skipping download.")
else:
    print(f"[INFO] {image_path} does not exist. Creating directory...")
    image_path.mkdir(parents=True, exist_ok=True)
-------------------code----------------------

#### **Download the Dataset**

Use the **raw** URL from GitHub (important: must use `raw.githubusercontent.com`, not `github.com/blob`):

-------------------code----------------------
url = "https://raw.githubusercontent.com/mrdbourke/pytorch-deep-learning/main/data/pizza_steak_sushi.zip"
target_file = data_path / "pizza_steak_sushi.zip"

with open(target_file, "wb") as f:
    print("[INFO] Downloading pizza_steak_sushi dataset...")
    request = requests.get(url)
    f.write(request.content)
-------------------code----------------------

#### **Unzip the Dataset**

-------------------code----------------------
with zipfile.ZipFile(target_file, "r") as zip_ref:
    print("[INFO] Unzipping dataset...")
    zip_ref.extractall(image_path)
-------------------code----------------------

#### **Final Structure Check**

Once unzipped, you should see:

-------------------code----------------------
data/
└── pizza_steak_sushi/
    ├── train/
    │   ├── pizza/
    │   ├── steak/
    │   └── sushi/
    └── test/
        ├── pizza/
        ├── steak/
        └── sushi/
-------------------code----------------------

---

### **4. Key Takeaways**

* Start small when working on ML projects.
* Use reproducible scripts to download and set up datasets.
* Always verify dataset structure before loading into PyTorch.
* The same setup applies to other custom datasets and domains (text, audio, etc.).

---

*** 137. Becoming One With the Data (Part 1): Exploring the Data Format ***
## Lecture Summary: Exploring and Preparing Custom Image Data for Classification in PyTorch

### 1. **Recap and Dataset Context**

* In the previous session, we downloaded a custom dataset based on the Food101 dataset.
* The dataset was reduced from 101 classes to 3: **pizza**, **steak**, and **sushi** for experimentation purposes.
* Dataset sourced from [TorchVision's Food101 dataset](https://pytorch.org/vision/stable/generated/torchvision.datasets.Food101.html).
* Data is arranged in the standard **image classification format**:

  -------------------code----------------------
  dataset/
  ├── train/
  │   ├── pizza/
  │   ├── steak/
  │   └── sushi/
  └── test/
      ├── pizza/
      ├── steak/
      └── sushi/
  -------------------code----------------------

---

### 2. **Section Introduction: Becoming One with the Data**

* This section focuses on **data exploration and preparation**.
* A humorous, fictional quote is referenced:
  *"If I had 8 hours to build a machine learning model, I'd spend 6 hours preparing my dataset." — Abraham Loss Function*

---

### 3. **Walking Through Directories**

* Python’s `os.walk()` is used to recursively explore dataset directories.
* The function returns a tuple:

  -------------------code----------------------
  (dirpath, dirnames, filenames)
  -------------------code----------------------
* This is useful for:

  * Counting the number of images per class
  * Verifying the directory structure

#### Example Output:

* `data/pizza_steak_sushi/` has 2 directories (`train/`, `test/`)
* `data/pizza_steak_sushi/test/steak` contains 19 test images
* `data/pizza_steak_sushi/train/` contains:

  * 78 pizza images
  * 75 steak images
  * 72 sushi images

---

### 4. **Dataset Preparation**

* Setting up dataset paths:

  -------------------code----------------------
  train_dir = "path/to/train"
  test_dir = "path/to/test"
  -------------------code----------------------
* This mirrors standard format expected by PyTorch's `ImageFolder`:

  * Root directory contains subfolders for each class
  * Class name = folder name
  * Images for each class go inside corresponding folders

---

### 5. **Why Use This Format?**

* PyTorch's `torchvision.datasets.ImageFolder` expects this format by default.
* Advantages:

  * Easy to load with built-in tools
  * Simple structure
  * Easily scalable (e.g., to 101 classes)

---

### 6. **Next Steps**

* Visualize images for further understanding.
* Write PyTorch code to convert images into tensors.
* Continue building the data input pipeline for a computer vision model.

---

*** 138. Becoming One With the Data (Part 2): Visualizing a Random Image ***
## **Lecture Summary: Visualizing and Exploring Image Data**

### **Recap**

* Dataset structure:

  * Around 75 training images and 25 testing images per class.
  * Each image class (e.g., steak, pizza, sushi) is placed in its respective folder:

    -------------------code----------------------
    train/steak/
    train/pizza/
    test/steak/
    test/pizza/
    -------------------code----------------------
  * This is the standard structure for image classification tasks.
  * When creating your own dataset, follow the same directory-based organization by class.

---

### **Objective**

Transform image files into tensors, but first, **explore and visualize** the data.

---

### **Plan to Visualize a Random Image**

1. **Get all image paths** using `pathlib.Path.glob()`.
2. **Pick a random image** path using `random.choice()`.
3. **Extract the image class name** (i.e., folder name) using `.parent.stem`.
4. **Open the image** using the Pillow library (`PIL.Image.open()`).
5. **Print image metadata** (class name, width, height).
6. **Display the image**.

---

### **Implementation Details**

#### 1. **Get All Image Paths**

-------------------code----------------------
from pathlib import Path

image_path_list = list(Path("data/images").glob("*/*/*.jpg"))
-------------------code----------------------

* `*/*/*.jpg`: Matches class folders in train/test sets and all `.jpg` images.
* Helps load all images across different categories and dataset splits.

#### 2. **Set Random Seed**

-------------------code----------------------
import random
random.seed(42)
-------------------code----------------------

* Ensures reproducibility by selecting the same random image.

#### 3. **Select a Random Image**

-------------------code----------------------
random_image_path = random.choice(image_path_list)
-------------------code----------------------

#### 4. **Get Image Class Name**

-------------------code----------------------
image_class = random_image_path.parent.stem
-------------------code----------------------

#### 5. **Open the Image**

-------------------code----------------------
from PIL import Image
img = Image.open(random_image_path)
-------------------code----------------------

* Pillow (`PIL`) is used to open and process images.

#### 6. **Print Metadata**

-------------------code----------------------
print(f"Path: {random_image_path}")
print(f"Class: {image_class}")
print(f"Width: {img.width}, Height: {img.height}")
-------------------code----------------------

#### 7. **Display the Image**

-------------------code----------------------
img.show()
-------------------code----------------------

---

### **Why Visualize Randomly?**

* Encourages better understanding of the dataset.
* Random selection helps uncover anomalies or edge cases.
* Important step before training to avoid surprises later.

---

### **Note on Data Visualization**

* For other domains (e.g., text, audio), similar methods exist:

  * Listen to random audio
  * Read random text
* Use domain-specific libraries for loading and displaying data.

---

### **Challenge Before Next Lecture**

**Try to visualize a random image using Matplotlib** instead of `img.show()`:

* Use `matplotlib.pyplot.imshow()`
* Keep randomness and metadata output

---


*** 139. Becoming One With the Data (Part 3): Visualizing a Random Image with Matplotlib ***

## **Lecture Summary: Visualizing Images with Matplotlib**

### **Recap**

* Previously visualized images using the Pillow (`PIL`) library.
* Challenge from the last lecture: replicate the image visualization using **Matplotlib**.
* Goal: Understand how to work with image arrays and prepare for PyTorch.

---

## **Why Use Matplotlib and NumPy?**

* **Matplotlib** is one of the most widely used libraries for data visualization in Python.
* **NumPy** is needed to convert the image object into an array format for plotting.

---

## **Steps to Visualize an Image with Matplotlib**

### 1. **Convert Image to NumPy Array**

-------------------code----------------------
import numpy as np

image_as_array = np.array(image)
-------------------code----------------------

* `image` is the object previously opened using Pillow.
* Converts the image to a numeric array format that Matplotlib can render.

### 2. **Plot the Image Using Matplotlib**

-------------------code----------------------
import matplotlib.pyplot as plt

plt.figure(figsize=(10, 7))
plt.imshow(image_as_array)
plt.title(f"{image_class} | Shape: {image_as_array.shape}")
plt.axis('off')
plt.show()
-------------------code----------------------

* `image_class` is the class label (e.g., "pizza", "steak").
* `image_as_array.shape` returns a tuple in the format: `(height, width, channels)`.

---

## **Understanding Image Shape and Format**

* Example shape: `(512, 363, 3)`

  * **512** = height in pixels
  * **363** = width in pixels
  * **3** = number of color channels (RGB)

### **Channel Format**

* **Pillow & Matplotlib**: Use **channels last** format `(H, W, C)`
* **PyTorch**: Uses **channels first** format `(C, H, W)`

> It's essential to understand shape formatting to prevent mismatched tensor shape errors during model training.

---

## **Summary and Next Steps**

* Visualizing random images is a powerful technique for better data understanding.
* This process applies across domains (e.g., audio, text).
* Now that we've explored the structure and shape of our data:

  * Next step: **Convert all images into PyTorch tensors**
  * Images have varying height and width but consistently 3 color channels.

---

*** 140. Transforming Data (Part 1): Turning Images Into Tensors ***
# Lecture Notes: Transforming Data for PyTorch

## 1. Introduction

* Previously, we converted an image to a NumPy array, illustrating how images can be represented as arrays.
* In this lecture, we aim to load images from a **custom dataset** (e.g., Pizza, Steak, Sushi) and transform them for use in PyTorch.

---

## 2. Goal

To use image data with PyTorch, we need to:

1. Convert data into **tensors**.
2. Wrap data in a **`torch.utils.data.Dataset`** object.

Then, use:

* **`DataLoader`** to create an iterable or batchable dataset.

This applies not only to images but also to text, audio, and other data types.

---

## 3. TorchVision Dataset and Transform

### 3.1 PyTorch Datasets

* PyTorch provides built-in datasets and base classes to define **custom datasets**.
* Use **`torchvision.datasets.ImageFolder`** for structured image directories.
* The `transform` parameter is key for preprocessing the data.

---

## 4. Applying Transforms

### 4.1 Why Use Transforms?

* Transforms prepare raw data for use with models.
* You can chain multiple transforms to manipulate the data before feeding it into the model.
* Common goals: resizing, normalization, tensor conversion, augmentation.

---

## 5. Example: Defining a Transform Pipeline

-------------------code----------------------
from torchvision import datasets, transforms
from torch.utils.data import DataLoader
-------------------code----------------------

### 5.1 Define a Transform

-------------------code----------------------
data_transform = transforms.Compose([
    transforms.Resize((64, 64)),                      # Resize to 64x64
    transforms.RandomHorizontalFlip(p=0.5),           # Augment with random flip
    transforms.ToTensor()                             # Convert to tensor (0–1 range)
])
-------------------code----------------------

### Explanation:

* **`Resize((64, 64))`**: Matches input size expected by some CV models (e.g., TinyVGG).
* **`RandomHorizontalFlip(p=0.5)`**: Adds variation to training data via augmentation.
* **`ToTensor()`**: Converts a PIL image or NumPy array to a `torch.FloatTensor` with shape \[C, H, W] and values scaled from \[0, 255] → \[0, 1].

---

## 6. Testing the Transform

### Single Image Test

-------------------code----------------------
# Assuming `img` is a PIL image:
transformed_img = data_transform(img)
print(transformed_img.shape)     # Expected: [3, 64, 64]
print(transformed_img.dtype)     # Expected: torch.float32
-------------------code----------------------

* You must pass a **PIL image**, not a NumPy array.
* This pipeline resizes, optionally flips, and converts the image.

---

## 7. Next Step

* Currently, the transform is applied to a **single image**.
* In the next part, we’ll apply transforms to **all images** in a folder and visualize the results.
* We’ll compare original vs. transformed images side by side.

---

*** 141. Transforming Data (Part 2): Visualizing Transformed Images ***
## **Lecture Summary: Visualizing Transformed Images in PyTorch**

### **1. Purpose of Image Transforms**

* Transforms help prepare images to be used with models (e.g., converting them into PyTorch tensors, resizing, augmentations).
* Transforms can be chained using `Compose`.
* TorchVision provides many predefined transforms including resizing, flipping, cropping, grayscale, etc.
* Visualizing transformed images is essential to understand the effect of data augmentation.

### **2. Documentation and Further Reading**

* Refer to the [TorchVision Transforms Documentation](https://pytorch.org/vision/stable/transforms.html) for a complete list.
* Check the [Illustration of Transforms](https://pytorch.org/vision/stable/transforms.html#functional-transforms) for visual examples.

### **3. Goal of This Section**

* Visualize original vs. transformed images side-by-side to understand the impact of transformations.

---

## **4. Function to Visualize Transformed Images**

### **Function Parameters:**

* `image_paths`: List of image paths.
* `transform`: The transform function to apply.
* `n`: Number of images to visualize.
* `seed`: Optional seed for randomness.

### **Function Behavior:**

* Randomly selects `n` image paths from the list.
* For each selected image:

  * Opens the image using PIL.
  * Displays original and transformed image side-by-side using Matplotlib.
  * Applies transform and handles dimension reordering (for Matplotlib: channels last format).

### **Common Error and Fix:**

* **Error**: Invalid shape due to channel order.
* **Fix**: Use `permute()` to change from `[C, H, W]` to `[H, W, C]`.

---

## **5. Example Use Case**

-------------------code----------------------
visualize_transformed_images(
    image_paths=image_path_list,
    transform=data_transform,
    n=3,
    seed=42
)
-------------------code----------------------

* **`data_transform`** used includes:

  * Resizing image to 64x64
  * Random horizontal flipping
  * Converting to tensor

### **Observations from Output:**

* Images resized to 64x64 show pixelation but maintain recognizable structure.
* Horizontal flip applied to some images.
* Final output shape is consistent with the expected input shape for CNN models (e.g., from the CNN Explainer site).

---

## **6. Why Resize to 64x64?**

* Smaller image size reduces computational cost.
* Risk of losing important features.
* Image size can be tuned as a hyperparameter.

---

## **7. Key Takeaways**

* Always visualize your data transformations before feeding them into a model.
* Use transforms to enrich training data and normalize inputs.
* Explore available transforms and understand their effect on your dataset.
* Set a seed when you want reproducible augmentations.
* Maintain shape compatibility when visualizing tensors.

---

## **Next Steps**

* The transform defined here will be reused when loading all dataset images into a TorchVision dataset in the next lecture.
* Visualization helps confirm the correctness of the transform pipeline before integration.

---

*** 142. Loading All of Our Images and Turning Them Into Tensors With ImageFolder ***
# **Part 4: Loading Image Data using `ImageFolder`**

## **Overview**

* We've transformed original images into tensors — the required format for PyTorch models.
* These transformations include resizing, random horizontal flipping, and conversion to tensors.
* Visualizing these transformed images confirms the transformations were applied correctly.

---

## **Using `ImageFolder` from `torchvision.datasets`**

### **What is `ImageFolder`?**

* A utility for loading image data stored in the **standard image classification format**:

  -------------------code----------------------
  root/class_x/xxx.png
  root/class_x/xxy.png
  root/class_y/xxz.png
  -------------------code----------------------
* Automatically assigns labels based on folder names.
* Comes with helpful attributes (e.g., class names, targets, samples).

---

## **Code Walkthrough: Loading the Training and Test Data**

### **1. Import Required Module**

-------------------code----------------------
from torchvision import datasets
-------------------code----------------------

### **2. Load Training Data**

-------------------code----------------------
train_data = datasets.ImageFolder(
    root=train_dir,
    transform=data_transform,
    target_transform=None  # optional; default is None
)
-------------------code----------------------

### **3. Load Test Data**

-------------------code----------------------
test_data = datasets.ImageFolder(
    root=test_dir,
    transform=data_transform,
    target_transform=None
)
-------------------code----------------------

---

## **Understanding Transforms**

* `transform`: Applied to the image (resizing, flipping, tensor conversion).
* `target_transform`: Optional transform for labels (e.g., if label manipulation is needed).

Since our labels are inferred from folder names, we do not need `target_transform`.

---

## **Inspecting the Datasets**

### **Dataset Summary**

-------------------code----------------------
print(train_data)
print(test_data)
-------------------code----------------------

* Shows number of samples, root directory, and applied transforms.

### **Class Names**

-------------------code----------------------
print(train_data.classes)
-------------------code----------------------

* Output: `['pizza', 'steak', 'sushi']`

### **Class-to-Index Mapping**

-------------------code----------------------
print(train_data.class_to_idx)
-------------------code----------------------

* Output: `{'pizza': 0, 'steak': 1, 'sushi': 2}`

### **Dataset Length**

-------------------code----------------------
len(train_data)
len(test_data)
-------------------code----------------------

---

## **Exploring Dataset Internals**

### **Samples and Targets**

* `train_data.samples`: List of (image path, label) tuples.
* `train_data.targets`: List of labels in order.

### **Example**

-------------------code----------------------
print(train_data.samples[0])
# Output: ('path/to/train/pizza/image.png', 0)
-------------------code----------------------

---

## **Visualization**

* Important for understanding what transformations do to the images.
* Example: Display a few samples from `train_data` with their labels.

---

## **Conclusion**

* `ImageFolder` simplifies loading datasets formatted for image classification.
* Transforms are crucial for preprocessing and data augmentation.
* Many useful attributes are built into `ImageFolder`, allowing for easy exploration and integration.

---


*** 143. Visualizing a Loaded Image From the Train Dataset *** 
# **Lecture Summary: Visualizing and Preparing Image Data for PyTorch Models**

## **Recap**

* In the previous video, `ImageFolder` was used to load image data and apply a series of transformations using a transform pipeline:

  * Resize to (64, 64)
  * Random horizontal flip
  * Convert to PyTorch tensor
* The resulting dataset is now fully compatible with PyTorch models.

---

## **Visualizing a Sample from the Training Dataset**

### **Accessing Image and Label**

-------------------code----------------------
image, label = train_data[0]
-------------------code----------------------

* This returns:

  * `image`: a tensor representing the image.
  * `label`: a numeric index corresponding to a class.

### **Converting Numeric Label to String**

-------------------code----------------------
class_names[label]
-------------------code----------------------

* Converts the numeric label back to a human-readable class name (e.g., "pizza").

---

## **Inspecting the Tensor**

-------------------code----------------------
print(f"Image Tensor:\n{image}")
print(f"Image Shape: {image.shape}")
print(f"Image Dtype: {image.dtype}")
print(f"Label: {label}")
print(f"Label Type: {type(label)}")
-------------------code----------------------

### **Key Takeaways**

* Image shape: `torch.Size([3, 64, 64])` — Channels First (C, H, W)
* Image dtype: `torch.float32` — Default in PyTorch
* Label: Integer
* Label type: `<class 'int'>`

**Note**: Shape, dtype, and device mismatches are common sources of bugs in PyTorch workflows.

---

## **Plotting the Image using Matplotlib**

### **Permute Tensor for Plotting**

-------------------code----------------------
image_permuted = image.permute(1, 2, 0)
-------------------code----------------------

* Converts shape from `[C, H, W]` to `[H, W, C]` which is required by Matplotlib.

### **Print New Shape**

-------------------code----------------------
print(f"Permuted Shape: {image_permuted.shape}")
-------------------code----------------------

### **Plot the Image**

-------------------code----------------------
plt.figure(figsize=(10, 7))
plt.imshow(image_permuted)
plt.axis(False)
plt.title(class_names[label], fontsize=14)
plt.show()
-------------------code----------------------

* Displays the image with class label as the title.

---

## **Suggestions for Practice**

* Try visualizing a **random image** from the dataset instead of the first one.
* Modify the **transform pipeline** to experiment with different preprocessing techniques:

  * Increase image size.
  * Add new transforms from `torchvision.transforms`.

---

## **Next Steps**

* Convert the dataset into a **DataLoader** for batch processing:

  * Allows shuffling, parallel loading, and batching of the dataset.
  * Recommended for efficient training of PyTorch models.

### **Encouragement for Practice**

* Try writing your own `DataLoader`:

  -------------------code----------------------
  from torch.utils.data import DataLoader

  train_dataloader = DataLoader(train_data, batch_size=32, shuffle=True)
  test_dataloader = DataLoader(test_data, batch_size=32)
  -------------------code----------------------
* This is an essential step in preparing data for training neural networks.

---

*** 144. Turning Our Image Datasets into PyTorch Dataloaders ***
## Lecture Summary: Turning Datasets into DataLoaders in PyTorch

### Overview

This lecture continues the PyTorch workflow by converting custom datasets into **DataLoaders**. The DataLoader allows for batching, shuffling, and parallel data loading—crucial for training efficiency and hardware limitations.

---

### Why Use a DataLoader?

* **Purpose**: Converts a dataset into an iterable that supports batching and parallel processing.
* **Batching**: Helps train the model with smaller, manageable data chunks (batches).
* **Memory Management**:

  * Loading large datasets all at once (e.g., 100,000 images) may exhaust memory.
  * DataLoader mitigates this by loading only a batch at a time.
  * Example: A GPU with 15–16 GB memory (e.g., Tesla T4) can process small batches efficiently without running out of memory.

---

### Key Concepts

* **Batch Size**: Number of samples processed before updating the model.

  * Common value: 32 (used in the example).
  * Demonstrated starting with 1 for clarity.

* **num\_workers**: Controls parallel data loading using CPU cores.

  * More workers generally improve loading speed.
  * Use `os.cpu_count()` to determine available CPU cores.
  * Example:

    -------------------code----------------------
    import os
    os.cpu_count()
    -------------------code----------------------

* **Shuffling**:

  * For **training data**: Set `shuffle=True` to avoid learning the order.
  * For **test data**: Set `shuffle=False` for consistent evaluation.

---

### Code Summary

#### Import and Set Batch Size

-------------------code----------------------
from torch.utils.data import DataLoader
BATCH_SIZE = 32  # or start with 1
-------------------code----------------------

#### Create DataLoaders

-------------------code----------------------
train_dataloader = DataLoader(
    dataset=train_data,
    batch_size=BATCH_SIZE,
    num_workers=1,         # or use os.cpu_count()
    shuffle=True
)

test_dataloader = DataLoader(
    dataset=test_data,
    batch_size=BATCH_SIZE,
    num_workers=1,
    shuffle=False
)
-------------------code----------------------

#### Result

* `train_dataloader` and `test_dataloader` are now iterable.
* Batch size of 1 gives the same length as the dataset.
* Larger batch sizes reduce the number of iterations (e.g., 225 images → \~8 batches with batch size 32).

---

### Visualizing Data Shapes

To inspect the shape of data from the DataLoader:

-------------------code----------------------
images, labels = next(iter(train_dataloader))
print(images.shape)  # [batch_size, channels, height, width]
print(labels.shape)  # [batch_size]
-------------------code----------------------

* With batch size 1:

  * Images shape: `[1, 3, 64, 64]`
  * Labels shape: `[1]`
* With batch size 32:

  * Images shape: `[32, 3, 64, 64]`
  * Labels shape: `[32]`

---

### Recap

* You’ve built a complete PyTorch pipeline:

  1. Downloaded a custom dataset.
  2. Loaded it using `ImageFolder`.
  3. Applied transformations.
  4. Converted the dataset to batches using `DataLoader`.

---

### Next Steps

In the following lecture:

* Explore how to build your own dataset class (simulating `ImageFolder`) from scratch.
* Learn to integrate it with `DataLoader` when no helper classes are available.

---

*** 145. Creating a Custom Dataset Class in PyTorch High Level Overview ***
# Lecture Notes: Creating a Custom Dataset Class in PyTorch

## Overview

In previous lessons, we used `ImageFolder` from `torchvision.datasets` to load image data and convert it into tensors. However, in this session, we explore what to do **if `ImageFolder` didn't exist**—a scenario that encourages a deeper understanding of PyTorch’s dataset utilities by **building a custom dataset class from scratch**.

---

## Goal

To replicate the core functionality of `ImageFolder`:

1. **Load images from files**
2. **Extract class names from folder structure**
3. **Create a mapping from class names to class indices (dictionary)**
4. **Make the dataset compatible with PyTorch's `DataLoader`**

---

## Section 5: Option 2 – Loading Image Data with a Custom Dataset

### Functional Requirements

We want our custom dataset class to:

* Load image data from a directory.
* Extract class names as a **list**.
* Create a **dictionary** that maps class names to indices.

---

## Pros and Cons of Custom Datasets

### Pros

* **Flexibility**: You can create datasets from virtually any data source.
* **Independence**: You're not limited by PyTorch's built-in utilities.

### Cons

* **Complexity**: More code increases the risk of bugs or inefficiencies.
* **Robustness**: Prebuilt tools are battle-tested; custom code usually isn’t at first.

---

## PyTorch Dataset Foundation

* All custom datasets should inherit from `torch.utils.data.Dataset`.
* You must **override**:

  * `__getitem__(self, index)`
  * `__len__(self)`

This base class is the foundation for many standard datasets in PyTorch.

---

## Preparing for the Custom Dataset

### Required Imports

-------------------code----------------------
import os
from pathlib import Path
from PIL import Image
import torch
from torch.utils.data import Dataset
from torchvision import transforms
from typing import Tuple, Dict, List
-------------------code----------------------

### Why These Imports?

* `os` / `pathlib`: For file system navigation.
* `PIL.Image`: To open image files.
* `torch` & `Dataset`: For PyTorch dataset compatibility.
* `transforms`: To convert images to tensors.
* `typing`: For type annotations and cleaner code structure.

---

## Target Functionality to Replicate

We aim to recreate `ImageFolder`-like behavior, particularly:

* `.classes`: List of class names
* `.class_to_idx`: Dictionary mapping class names to indices

Given a directory structure like:

-------------------code----------------------
data/
  pizza/
  steak/
  sushi/
-------------------code----------------------

We should be able to:

* Get class names: `['pizza', 'steak', 'sushi']`
* Get class-to-index mapping: `{'pizza': 0, 'steak': 1, 'sushi': 2}`

---

## Summary

This lecture serves as the foundation for building a **custom PyTorch Dataset**. We've:

* Reviewed why and when to create custom datasets.
* Identified the core components needed to replicate `ImageFolder`.
* Outlined the next steps: building helper functions and subclassing `Dataset`.

---

## Next Steps

In the following video, we will:

* Start coding a helper function that:

  * Accepts a file path
  * Returns a list of class names
  * Returns a dictionary mapping class names to indices

This helper function will be a building block for our custom dataset class.

---

*** 146. Creating a Helper Function to Get Class Names From a Directory ***
# Lecture 5.1: Creating a Helper Function to Get Class Names

## Objective

Create a helper function that mimics `ImageFolder`'s functionality by:

1. Scanning a target directory for class folders
2. Returning a **list of class names**
3. Returning a **dictionary mapping class names to indices**

This function will serve as a building block for our custom dataset class.

---

## Key Assumptions

* The directory structure follows the **standard image classification format**:

  -------------------code----------------------
  root/
    class1/
      img1.jpg
      img2.jpg
    class2/
      img1.jpg
      ...
  -------------------code----------------------

* Example usage directory:

  -------------------code----------------------
  data/train/pizza/
  data/train/steak/
  data/train/sushi/
  -------------------code----------------------

---

## Implementation Plan

### Step 1: Use `os.scandir()` to Traverse the Directory

We scan the target directory and extract class names from the folder names (not filenames).

### Step 2: Raise an Error If No Class Folders Are Found

We ensure the directory structure is valid. If no folders are found, raise a `FileNotFoundError`.

### Step 3: Return Both a List and a Dictionary

Return:

* `class_names`: A sorted list of class folder names.
* `class_to_idx`: A dictionary mapping each class name to a unique integer.

---

## Function Definition

-------------------code----------------------
from typing import List, Dict, Tuple
import os

def find_classes(directory: str) -> Tuple[List[str], Dict[str, int]]:
    """
    Finds class folder names in a target directory.

    Args:
        directory (str): Path to the dataset root (e.g., data/train).

    Returns:
        Tuple containing:
            - A sorted list of class names.
            - A dictionary mapping class names to integer indices.
    """

    # Step 1: Get class folder names
    classes = sorted(
        entry.name for entry in os.scandir(directory) if entry.is_dir()
    )

    # Step 2: Raise error if no class directories found
    if not classes:
        raise FileNotFoundError(
            f"Couldn't find any class folders in directory: {directory}. "
            "Please check the file structure."
        )

    # Step 3: Create dictionary mapping class names to indices
    class_to_idx = {class_name: i for i, class_name in enumerate(classes)}

    return classes, class_to_idx
-------------------code----------------------

---

## Example Usage

-------------------code----------------------
train_dir = "data/train"
class_names, class_to_idx = find_classes(train_dir)

print(class_names)      # ['pizza', 'steak', 'sushi']
print(class_to_idx)     # {'pizza': 0, 'steak': 1, 'sushi': 2}
-------------------code----------------------

---

## Summary

* This helper function replicates part of the functionality of `torchvision.datasets.ImageFolder`.
* It will be used as a utility within our custom dataset class to handle class discovery and indexing.
* Ensures modular, reusable, and testable code for different datasets.

---

## Next Steps

In the next lecture, we will:

* Begin writing our custom dataset class
* Subclass `torch.utils.data.Dataset`
* Integrate this helper function into the dataset initialization logic
