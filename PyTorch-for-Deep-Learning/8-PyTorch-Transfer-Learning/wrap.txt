*** 181. Introduction: What is Transfer Learning and Why Use It ***
## **Lecture Summary: Transfer Learning**

### **What is Transfer Learning?**

* Transfer learning is the process of taking a machine learning model trained on one task and adapting it to a different, but related, task.
* Instead of building models from scratch, we reuse the learned patterns (weights/parameters) from an existing model trained on a large dataset.
* This is especially useful when you don’t have a massive dataset for your specific problem.

---

### **Example Use Case: Computer Vision**

* **Task**: Classify food images (e.g., Food Vision Mini).
* **Source Model**: A model trained on the **ImageNet** dataset (over 1 million images, 1,000 classes).
* **Approach**:

  * Use the patterns learned from ImageNet.
  * Customize (fine-tune) those patterns for the food classification task.
* **Benefit**: The model already knows general visual features (edges, textures, object shapes), which can be fine-tuned for more specific use cases.

---

### **Example Use Case: Natural Language Processing**

* **Task**: Build a spam detection model.
* **Source Model**: A language model trained on **Wikipedia**.
* **Approach**:

  * Use the pre-trained model’s understanding of language structure (syntax, semantics).
  * Customize it for email classification (e.g., spam vs. not spam).
* **Example**: In a sentence like *“The \_\_\_ jumped over the fence”*, the model might predict “dog” as a likely word due to learned context.

---

### **Key Concept**

* **Transfer Learning** = Pre-train a model on a large, general dataset → Fine-tune on a smaller, task-specific dataset.

---

### **Why Use Transfer Learning?**

#### **Main Benefits**

1. **Leverage Proven Architectures**

   * Use models that have been carefully designed and tested (e.g., EfficientNet, ResNet, Vision Transformers).
2. **Reuse Learned Patterns**

   * Instead of learning from scratch, start with pre-learned features that are often generalizable.
3. **Save Resources**

   * Requires **less data**, **less compute**, and **less time** to get good performance.
4. **Achieve Better Results**

   * Often leads to better model performance compared to training from scratch.

---

### **Research Support**

#### **From “How to Train Your ViT” (Vision Transformer)**

* Most practitioners **use publicly available pre-trained models** rather than training from scratch.
* **Key Quote**:

  > “Transfer learning remains the best available option, even if the downstream data is only weakly related to the pretraining data.”

---

### **Expert Insight**

* **Jeremy Howard (Founder of FastAI)** in a podcast with Lex Fridman:

  > “The things that really make a difference? Transfer learning. It’s a world-changing thing. More people can do world-class work with fewer resources and less data.”

---

### **Model Improvement Reminder**

From previous lessons on improving models:

* Options to improve include:

  * Add more data
  * Use better data
  * Apply data augmentation
  * **Use transfer learning**

---

### **Next Step**

* In the following session: Learn where to find **publicly available pre-trained models**.

---

*** 182. Where Can You Find Pre-trained Models and What We Are Going to Cover ***
### **Lecture Summary: Transfer Learning & Pre-trained Models**

#### **1. What is Transfer Learning and Why Use It?**

* **Definition**: Transfer learning involves using pre-trained models on new tasks.
* **Benefits**:

  * Enables individuals with fewer resources and less data to perform world-class machine learning work.
  * Saves time and effort by leveraging existing knowledge.

---

#### **2. Sources for Pre-trained Models**

* **Main Resources**:

  * **PyTorch Domain Libraries**:

    * `torchvision` for vision models
    * `torchtext` for text models
    * `torchaudio` for audio models
    * `torchrec` for recommendation models
  * **Torch Image Models (TIMM)**:

    * A powerful library for computer vision models.
  * **Hugging Face Hub**:

    * Offers a wide range of models across modalities (vision, NLP, audio, etc.).
  * **Papers with Code (SOTA - State of the Art)**:

    * Source for the latest top-performing models and benchmarks in various ML domains.

---

#### **3. Deep Dive: TorchVision Models**

* **Accessing Pre-trained Models**:

  * Go to the PyTorch documentation, specifically `torchvision.models`.
  * Use models with pre-trained weights for tasks like image classification.
* **Model Examples**:

  * AlexNet, DenseNet, EfficientNet V2, MobileNet V3, Swin Transformer, Vision Transformer, etc.
* **Version Info**:

  * The lecture references `torchvision` version 0.12.
  * An unstable (future) release is being used for the demonstration (as of June 24, 2022).

---

#### **4. Why Use These Resources?**

* They provide access to top-tier models without needing to train from scratch.
* Ideal for experimenting and applying models to real-world problems efficiently.
* Great starting point for benchmarking and understanding what already works.

---

#### **5. Specific Project Context**

* **Project**: *FoodVision Mini* — an image classification model for food items.
* **Dataset**: Images of pizza, steak, and sushi.
* **Objective**: Build and optimize a transfer learning-based classifier using PyTorch.
* **Steps**:

  * Import modular code from previous sections.
  * Apply transfer learning concepts using PyTorch code.
  * Customize and fine-tune a pre-trained model.
  * Evaluate model performance.
  * Make predictions on custom input data.

---

#### **6. Final Notes**

* Focus is on PyTorch and its domain libraries.
* Encouraged to explore other resources like Hugging Face and Papers with Code independently.
* Suggestions or additional resources can be shared in the course’s GitHub discussions.

---


*** 183. Installing the Latest Versions of Torch and Torchvision ***
### **Lecture Summary: PyTorch Transfer Learning Setup in Google Colab**

#### **1. Starting the Coding Environment**

* The coding is done in **Google Colab**:
  [https://colab.research.google.com](https://colab.research.google.com)
* A new Colab notebook is created and titled:
  `06 - PyTorch Transfer Learning`
* The heading inside the notebook is also titled accordingly for clarity.

---

#### **2. Understanding Transfer Learning**

* **Definition**: Transfer learning involves using a model pre-trained on one dataset and adapting it to a new problem.
* **Terminology**:

  * **Pre-trained models**: Already trained models reused for similar tasks.
  * **Foundation models**: A broader term used increasingly; often interchangeable with pre-trained models.

---

#### **3. Setting Up the Environment**

* **Runtime Configuration**:

  * Ensure GPU is enabled: `Runtime > Change Runtime Type > GPU`.

* **PyTorch and TorchVision Versions**:

  * The goal is to use **PyTorch ≥ 1.12** and **TorchVision ≥ 0.13**.
  * At the time of recording:

    * The stable version is **TorchVision 0.12**.
    * The unstable (beta) version **0.13** includes valuable API updates (e.g., multi-weight support).
    * Eventually, this unstable version will become stable and standard.

---

#### **4. Multi-weight Support in TorchVision**

* **Purpose**:

  * Enables use of the same architecture with different pre-trained weights.
  * Facilitates experimentation and flexibility with pre-trained models.

---

#### **5. Verifying Current Versions**

* Check versions with:

  -------------------code----------------------
  import torch
  import torchvision
  print(torch.__version__)
  print(torchvision.__version__)
  -------------------code----------------------

* If versions are **not compatible**, upgrade is necessary.

---

#### **6. Installing Nightly Versions**

* **Installation Block**:

  * Installs nightly (development) versions of PyTorch and TorchVision via pip.
  * Automatically checks versions and installs only if needed.
  * TorchAudio is **not required** here.

* **Installation Steps**:

  * Downloads and installs new versions.
  * Uninstalls older versions first.
  * May take 4–5 minutes depending on connection speed.

---

#### **7. Restarting Runtime**

* After installation, the Colab runtime must be **manually restarted**.
* Re-run the version check code to verify successful installation:

  * PyTorch ≥ 1.12
  * TorchVision ≥ 0.13 (often shows as `dev` version with a timestamp)

---

#### **8. Notes and Warnings**

* **Future-proofing**:

  * Eventually, Google Colab will support newer versions by default.
* **Conditional Execution**:

  * If your versions are already compatible, **you can skip the installation block**.
  * Keep the code block available in case of future incompatibility.

---

#### **Next Step**

* Proceed to the next video for further setup and code implementation related to transfer learning.

---

*** 184. Downloading Our Previously Written Code from Going Modular ***
### **Lecture Summary: Setting Up for Transfer Learning in Google Colab**

#### **1. Purpose of the Setup**

* To **avoid rewriting code** from previous sections.
* Code from Section 5 (Going Modular) will be **imported as reusable scripts** into the current notebook.
* The goal is to prepare the environment for transfer learning efficiently.

---

#### **2. Importing Pre-Written Modular Code**

* From the previous modularization step, the following scripts are available:

  * `dataset_setup.py`
  * `engine.py`
  * `model_builder.py`
  * `train.py`
  * `predict.py`
  * `utils.py`

* These scripts contain essential utilities for:

  * Loading datasets
  * Training and testing models
  * Making predictions

---

#### **3. Cloning the GitHub Repository**

* GitHub repo: **`PyTorch-Deep-Learning`**
* Directory of interest: **`going_modular/`**
* Steps performed:

  1. Clone the repo using `git clone`.
  2. Move the `going_modular/` directory into the working directory.
  3. Clean up unnecessary files from the repo.
  4. Import the required modules:

     -------------------code----------------------
     from going_modular import dataset_setup, engine
     -------------------code----------------------

---

#### **4. Installing Required Packages**

* The script attempts to import `torchinfo`.
* If `torchinfo` is not available, it is installed via:

  -------------------code----------------------
  pip install torchinfo
  -------------------code----------------------
* `torchinfo` helps visualize model summaries in PyTorch (similar to Keras' `model.summary()`).

---

#### **5. Verifying Script Access**

* After cloning and importing:

  * Confirm access to helper functions such as:

    * `dataset_setup.create_dataloaders()`
    * `engine.train()`
* These functions were defined in earlier notebooks and modularized for reuse.

---

#### **6. Setting Up Device-Agnostic Code**

* Enables the notebook to automatically choose **GPU if available**, otherwise CPU:

  -------------------code----------------------
  device = "cuda" if torch.cuda.is_available() else "cpu"
  -------------------code----------------------

* Check current device and GPU specs:

  -------------------code----------------------
  !nvidia-smi
  -------------------code----------------------

* Example output: **Tesla P100** with 16GB memory (using Google Colab Pro).

---

#### **7. Google Colab Considerations**

* Google Colab is used to access **free (or upgraded) GPU resources**.
* Colab Pro users are more likely to receive high-performance GPUs.
* Local setups can also work, but must be configured manually.

---

#### **8. Important Notes**

* When revisiting the notebook after a break:

  * **Rerun all setup and import cells** (runtime resets).
* Scripts and setup process enable efficient reuse of previously written code.

---

#### **Next Steps**

* Proceed to the next lecture where the **dataset (pizza, steak, sushi)** will be loaded for training the transfer learning model.

---

*** 185. Downloading Pizza, Steak, Sushi Image Data from Github ***
## **Lecture Summary: Downloading and Preparing the Dataset (Pizza, Steak, Sushi)**

### **1. Objective**

* Set up and prepare the **Pizza, Steak, Sushi** image dataset, a subset of the **Food101** dataset, for training.
* Practice downloading and extracting data using Python instead of relying on pre-cloned folders.

---

### **2. Cloning Consideration**

* Although the data is already available in the cloned `PyTorch-Deep-Learning` GitHub repo, the instructor simulates downloading and preparing it manually for learning purposes.
* Alternatives:

  * Keep both the `going_modular` and `data` folders from the repo.
  * Create a reusable function like `download_data(url)` for future datasets.

---

### **3. Required Libraries**

-------------------code----------------------
import os
import zipfile
import requests
from pathlib import Path
-------------------code----------------------

---

### **4. Define Paths**

* `data_path`: Root directory for all data.
* `image_path`: Directory containing the extracted images.

-------------------code----------------------
data_path = Path("data")
image_path = data_path / "pizza_steak_sushi"
-------------------code----------------------

---

### **5. Check if Dataset Already Exists**

-------------------code----------------------
if image_path.is_dir():
    print(f"[INFO] {image_path} directory exists, skipping download.")
else:
    print(f"[INFO] {image_path} not found, downloading...")
-------------------code----------------------

---

### **6. Downloading the Dataset**

* If not already present, create necessary directories.
* Download the `.zip` file using `requests` and save it locally.

-------------------code----------------------
image_path.mkdir(parents=True, exist_ok=True)

with open(data_path / "pizza_steak_sushi.zip", "wb") as f:
    request = requests.get("https://raw.githubusercontent.com/mrdbourke/pytorch-deep-learning/main/data/pizza_steak_sushi.zip")
    print("[INFO] Downloading pizza_steak_sushi.zip...")
    f.write(request.content)
-------------------code----------------------

---

### **7. Extracting the Zip File**

-------------------code----------------------
with zipfile.ZipFile(data_path / "pizza_steak_sushi.zip", "r") as zip_ref:
    print("[INFO] Unzipping pizza_steak_sushi.zip...")
    zip_ref.extractall(image_path)
-------------------code----------------------

---

### **8. Cleanup**

* Delete the zip file after extraction to save space:

-------------------code----------------------
os.remove(data_path / "pizza_steak_sushi.zip")
-------------------code----------------------

---

### **9. Inspecting the Dataset**

* Directory structure should now be:

-------------------code----------------------
data/
└── pizza_steak_sushi/
    ├── train/
    │   ├── pizza/
    │   ├── steak/
    │   └── sushi/
    └── test/
        ├── pizza/
        ├── steak/
        └── sushi/
-------------------code----------------------

* Example code to verify paths:

-------------------code----------------------
train_dir = image_path / "train"
test_dir = image_path / "test"

print(train_dir)
print(test_dir)
-------------------code----------------------

---

### **10. Instructor's Tips and Challenges**

* **Extension idea 1**: Write a function `download_data(url, extract_to)` that handles downloading and extracting.
* **Extension idea 2**: When cloning a GitHub repo, **preserve the `data/` folder** instead of deleting everything except `going_modular`.

---

### **Next Step**

* In the following lecture, you will **turn image folders into PyTorch DataLoaders** for training and testing.
* There will be a small trick involved in that process, which will be explained next.

---


*** 186. Turning Our Data into DataLoaders with Manually Created Transforms ***
## Lecture Notes: Creating Data Loaders and Manual Transforms for Pretrained Models

### **1. Context Recap**

* In the last session, we:

  * Downloaded the **Pizza, Steak, and Sushi** dataset.
  * Created directory paths for **train** and **test** sets.

---

### **2. Objective**

* Convert image data into **PyTorch data loaders** using a function called `create_dataloaders()` from `data_setup.py` in the `going_modular` directory.

---

### **3. Dataset Loader Setup**

* The `create_dataloaders()` function:

  * Takes in `train_dir`, `test_dir`, and a **transform**.
  * Uses `ImageFolder` to wrap datasets.
  * Returns: `train_dataloader`, `test_dataloader`, and `class_names`.

---

### **4. Image Transformations**

#### **Why Transforms Matter**

* **Important Principle**: When using a **pre-trained model**, your input data must be transformed in **the same way** the model’s training data was.
* Otherwise, performance will degrade due to distribution mismatch.

---

### **5. Two Ways to Define Transforms**

> Requires **torchvision ≥ 0.13**

#### 1. **Manual Transform Definition**

* You define the preprocessing pipeline.
* Advantage: **Flexible** — you can tailor it to your model or experiment.
* Disadvantage: **Error-prone** — mismatched transforms can hurt performance.

#### 2. **Automatic Transform from Model Weights**

* Transform is predefined based on the model's weights (discussed in next lecture).

---

### **6. Manual Transform Details**

#### **Transform Requirements**

For most torchvision pre-trained models:

* Images should be:

  * RGB (3 channels)
  * Resized to at least `224x224`
  * Pixel values scaled from 0–1
  * Normalized using ImageNet statistics:

    * **Mean**: `[0.485, 0.456, 0.406]`
    * **Std**: `[0.229, 0.224, 0.225]`

#### **Code Example**

-------------------code----------------------
from torchvision import transforms

manual_transforms = transforms.Compose([
    transforms.Resize((224, 224)),  # Resize images
    transforms.ToTensor(),          # Convert to tensor and scale to [0, 1]
    transforms.Normalize(
        mean=[0.485, 0.456, 0.406], 
        std=[0.229, 0.224, 0.225]
    )  # Match ImageNet distribution
])
-------------------code----------------------

#### **Usage with Data Loader Function**

-------------------code----------------------
from going_modular.going_modular import data_setup

train_dataloader, test_dataloader, class_names = data_setup.create_dataloaders(
    train_dir=train_dir,
    test_dir=test_dir,
    transform=manual_transforms
)
-------------------code----------------------

---

### **7. Why Use These Specific Values?**

* These **mean** and **standard deviation** values come from the **ImageNet dataset**.
* ImageNet is a massive image database used to train many pretrained models.
* Normalizing with these values ensures our data distribution matches what the model expects.

---

### **8. Summary**

* You now have a dataset on disk and are preparing it for a **pre-trained model**.
* Today, you learned how to:

  * Use `create_dataloaders()` with a custom transform.
  * Manually create transforms that align with pretrained model expectations.

---

### **Next Step**

* Explore **automatic transforms** using built-in support in `torchvision.models` and pretrained weights (e.g., using `.weights.transforms()`).

*** 187. Turning Our Data into DataLoaders with Automatic Created Transforms ***
## Lecture Summary: Preparing Data for Pre-trained Models (Torchvision v0.13+)

### Key Concepts

#### 1. **Importance of Matching Pre-trained Model Data Format**

* Pre-trained models expect input data in the same format as their training data.
* Mismatched preprocessing can lead to **performance degradation**.

---

### Manual Transforms

* You can manually create a transform pipeline using `transforms.Compose([...])`.
* Manual transforms offer **flexibility**: you can add any preprocessing, data augmentation, etc.
* **Risk**: If the transform doesn’t match the model’s expected input format, it may reduce performance.

---

### Automatic Transforms (Torchvision 0.13+)

Introduced in `torchvision v0.13`, this new feature lets you automatically create the correct transforms from the pre-trained model's weights.

#### Benefits:

* Ensures **consistency** with the model’s training.
* **Reduces errors** due to manual misconfiguration.
* Easily switch between models without rewriting preprocessing logic.

#### Usage:

1. **Install/check your torchvision version:**

   -------------------code----------------------
   import torchvision
   print(torchvision.__version__)  # Should be >= 0.13
   -------------------code----------------------

2. **Import model and weights:**

   -------------------code----------------------
   from torchvision.models import efficientnet_b0, EfficientNet_B0_Weights
   weights = EfficientNet_B0_Weights.DEFAULT  # Defaults to best available weights
   -------------------code----------------------

3. **Get automatic transforms:**

   -------------------code----------------------
   auto_transforms = weights.transforms()
   -------------------code----------------------

4. **Create data loaders with automatic transforms:**

   -------------------code----------------------
   from torchvision import datasets
   from torch.utils.data import DataLoader

   train_dataset = datasets.ImageFolder(root=train_dir, transform=auto_transforms)
   test_dataset = datasets.ImageFolder(root=test_dir, transform=auto_transforms)

   train_loader = DataLoader(train_dataset, batch_size=32, shuffle=True)
   test_loader = DataLoader(test_dataset, batch_size=32)
   -------------------code----------------------

---

### Why This Matters

* This approach simplifies the workflow and ensures **high accuracy** with the appropriate normalization, resizing, cropping, etc.
* Using `EfficientNet_B0_Weights.DEFAULT` leverages the version trained on **ImageNet-1K** (1000-class ImageNet dataset).

  * Top-1 Accuracy: \~77.6%
  * Top-5 Accuracy: \~93%

---

### EfficientNet Overview

* Model series: `B0` to `B7`
* **Bigger number = Larger model**
* **Tradeoff**:

  * Higher accuracy → Larger size → Slower inference/deployment
  * Start small (e.g., B0) and scale up **only if necessary**

---

### Extra Curriculum / Homework

* Read through:
  [Torchvision Models and Pre-trained Weights Documentation](https://pytorch.org/vision/stable/models.html)
* Focus on:

  * Understanding the `multi-weight support API`
  * Exploring different models and their available pre-trained weights

---

### Final Thoughts

* Always align your data preprocessing with how the model was trained.
* For models in `torchvision`, use the **automatic transform method via `.transforms()`** to avoid mismatches.
* This new API makes using state-of-the-art models **much more accessible and robust**.

---
