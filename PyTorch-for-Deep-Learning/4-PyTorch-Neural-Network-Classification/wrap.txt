*** 68. Introduction to Machine Learning Classification With PyTorch ***
### **Classification in Machine Learning**

#### **1. What is a Classification Problem?**
- Classification is one of the main types of problems in machine learning.
- It involves predicting a label or category for a given input.
- Common in everyday scenarios, often powered by machine learning models.

---

### **2. Real-World Examples**

- **Spam Detection**:
  - Input: An email message.
  - Output: "Spam" or "Not Spam".
  - This is an example of **Binary Classification** (only two possible classes).

- **Food Image Recognition**:
  - Input: A photo of food.
  - Output: "Sushi", "Steak", or "Pizza".
  - This is **Multi-class Classification** (more than two classes).

- **Wikipedia Tagging**:
  - Input: A Wikipedia article.
  - Output: Multiple relevant tags (e.g., "Deep Learning", "Artificial Intelligence").
  - This is **Multi-label Classification** (each input can have multiple labels).

---

### **3. Types of Classification Problems**

- **Binary Classification**:
  - Two possible output classes (e.g., spam vs. not spam, cat vs. dog).
  - Example: Distinguishing between dog and cat images.

- **Multi-class Classification**:
  - More than two classes.
  - Each input is assigned to one and only one class.
  - Example: Recognizing food types like sushi, steak, or pizza.
  - Large-scale example: ImageNet dataset with 1,000 classes.

- **Multi-label Classification**:
  - Inputs can belong to multiple classes simultaneously.
  - Example: Assigning multiple topic tags to a Wikipedia article.

---

### **4. Key Concepts to Be Covered**

- **Architecture of a Neural Network Classification Model**
  - How the model is structured to handle classification tasks.

- **Input and Output Shapes**
  - Inputs are typically numerical and structured in tensors.
  - Understanding the shape is crucial for building models.

- **Features and Labels**
  - Inputs = Features (e.g., image pixels, words)
  - Outputs = Labels (e.g., "spam", "not spam")

- **Creating Custom Data**
  - Generating and manipulating datasets to train models.

- **Model Training**
  - Using loss functions and optimizers specifically for classification.
  - Building training and testing loops.

- **Saving and Loading Models**
  - Persisting models for reuse without retraining.

- **Nonlinear Parity**
  - Understanding how non-linear decision boundaries work compared to linear ones.

- **Evaluation Methods**
  - Various metrics and tools to evaluate the performance of classification models.

---

### **Teaching Style**
- A mix of hands-on coding ("cook") and theoretical understanding ("scientist", "artist", "chemist").
- Focus on building a deep, practical understanding through code examples and intuition.

---

*** 69. Classification Problem Example: Input and Output Shapes ***
## Lecture Summary: Classification Inputs and Outputs

### Overview
In this lecture, we moved from a general understanding of classification problems to exploring the actual **inputs and outputs** involved in a classification task. The example used throughout is an app called **FoodVision**, designed to classify food photos.

---

### Classification Workflow
1. **Input**  
   - Raw data: Images (e.g., sushi, steak, pizza photos)
   - Must be numerically encoded for machine learning algorithms to process.
   - Common encoding for images:
     - Shape: **(width, height, color_channels)**  
     - Example: **(224, 224, 3)**  
       - 224 width, 224 height, 3 color channels (RGB)

2. **Machine Learning Algorithm**
   - A model (e.g., neural network) that processes the encoded input.
   - Can be custom-built (e.g., with PyTorch) or a pre-trained model (e.g., in transfer learning).

3. **Output**
   - Model predicts **probabilities** for each class (e.g., 0.9 for pizza, 0.05 for sushi, 0.05 for steak).
   - These are **not discrete labels** but **prediction probabilities**.
   - Final prediction is the class with the highest probability.

---

### Types of Classification Illustrated
- **Single-label, Multiclass Classification**  
  - One label per input image (e.g., either sushi, steak, or pizza).

---

### Input Encoding Details
- Image representation: Pixel values across 3 color channels (R, G, B).
- Image tensor shape example:  
  - **(batch_size, color_channels, width, height)**  
  - Example: **(32, 3, 224, 224)**  
    - 32 images per batch (common batch size).
    - 3 color channels.
    - 224 x 224 image resolution.

> Note: PyTorch uses the shape **(C, W, H)** (channels-first), but other libraries (like TensorFlow) may use **(W, H, C)**.

---

### Batch Size
- Common batch size: **32**
- Helps manage memory and computational limits.
- Based on community recommendations and performance balance.

---

### Output Shape
- Depends on the number of classes.
  - For 3 food classes (sushi, steak, pizza): output shape is **(batch_size, 3)**
  - For binary classification: output could be **(batch_size, 1)** or **(batch_size, 2)**

---

### Key Takeaways
- Inputs must be **numerically encoded** (tensors of pixel values).
- Outputs are **prediction probabilities** per class.
- Tensor shapes:
  - **Inputs**: `(batch_size, channels, width, height)`
  - **Outputs**: `(batch_size, number_of_classes)`
- The number of output classes is determined by the specific classification problem.
- Machine learning improves by iterating: add more data, retrain, and refine predictions.

---

### Coming Up Next
- High-level architecture of a classification model.
- Visualizing the flow of data through a neural network.

---

*** 70. Typical Architecture of a Classification Neural Network (Overview) ***

---

## Lecture Summary: Architecture of a Classification Model

### Overview
- Classification problems require:
  - **Numerical inputs** (e.g., pixel values, numerical features).
  - **Prediction probabilities** as outputs (not discrete values).

---

### Key Components of a Classification Model

#### 1. **Hyperparameters**
- **User-defined settings** that influence the model.
- Examples:
  - Number of input features
  - Number of neurons per hidden layer
  - Number of hidden layers
  - Learning rate
  - Batch size

---

#### 2. **Types of Classification**
- **Binary classification**: Two classes (e.g., yes/no, 0/1).
- **Multiclass classification**: More than two classes (e.g., sushi, steak, pizza).

---

#### 3. **Input Layer**
- Determined by number of features (e.g., for a health dataset: age, sex, height, weight, smoking status).
- All inputs must be **numerically encoded** (e.g., male = 0, female = 1).

---

#### 4. **Hidden Layers**
- Contain neurons (also known as units or nodes).
- Each layer performs a mathematical operation using activation functions.
- Example PyTorch syntax:
  - `nn.Linear()`
  - `nn.ReLU()`
- You can have multiple hidden layers (e.g., ResNet has up to 152 layers).

---

#### 5. **Output Layer**
- Determines the shape of the output:
  - **Binary classification**: 1 output (e.g., 0 or 1)
  - **Multiclass classification**: 1 output per class (e.g., 3 outputs for sushi, steak, pizza)

---

#### 6. **Activation Functions**
- **Hidden Layers**: Commonly use `ReLU` (Rectified Linear Unit)
- **Output Layer**:
  - Binary classification: `Sigmoid`
  - Multiclass classification: `Softmax`

---

#### 7. **Loss Functions**
- Measure how far off the model’s predictions are from the actual values.
  - Binary classification: `Binary Cross Entropy Loss`
  - Multiclass classification: `Cross Entropy Loss`

---

#### 8. **Optimizers**
- Adjust the model’s weights to reduce loss.
  - Common choices: 
    - `SGD` (Stochastic Gradient Descent)
    - `Adam`

---

#### 9. **Sequential Models**
- Often built using a sequential approach: the data flows through each layer in order.
- Example for multiclass classification:
  - Input layer (e.g., pixel values or features)
  - Hidden layers (with activations)
  - Output layer (3 output neurons for 3 classes)

---

### Final Notes
- This overview introduces terminology and structure.
- We’ll dive into coding the actual models starting in the next lecture.

---

*** 71. Making a Toy Classification Dataset ***
# Lecture Notes: Introduction to Classification with a Toy Dataset

## Overview of Classification
- **Classification** is the task of predicting whether something belongs to one class or another.
- There can be multiple classes depending on the problem.
- **Examples**:
  - Email: Spam or Not Spam
  - Photos: Dog, Cat, Pizza, Sushi, or Steak


## Machine Learning Workflow
- **First step**: Always start with **data**.
- Machine learning needs data to find patterns; without data, learning is not possible.

## Creating Custom Data
- We will **create a custom dataset** for practice purposes.
- **Libraries**:
  - **scikit-learn** is pre-installed in Google Colab.
  - scikit-learn is widely used for machine learning (not deep learning).
  - PyTorch focuses mainly on deep learning.

### Creating the Data
- We use **make_circles** from scikit-learn to generate data.
- Generate **1000 samples** with added **noise** (set to 0.3) and a **random_state** for reproducibility.
- Variables:
  - `X`: features
  - `y`: labels

### Understanding the Data
- **Length**:
  - `len(X)` = 1000
  - `len(y)` = 1000
- **Samples**:
  - Each sample in `X` has **2 features** (coordinate points).
  - `y` contains the corresponding **label** (either 0 or 1).

### Type of Classification
- Since labels are only **0** and **1**, this is a **binary classification** problem.
- If there were more than two classes (e.g., 0, 1, 2), it would be **multiclass classification**.

## Exploring the Data
- **Data Exploration** is important to understand patterns.
- **Tools used**:
  - **pandas**: Create a DataFrame for easy inspection.
  - **matplotlib**: For data visualization (scatter plot).

### Creating a DataFrame
- Create a pandas DataFrame with:
  - `x1`: first feature
  - `x2`: second feature
  - `label`: the corresponding label

### Visualizing the Data
- **Scatter Plot**:
  - X-axis: `x1`
  - Y-axis: `x2`
  - Points colored based on the `label` (using a color map: red-yellow-blue).
- Visualization shows two distinct **circles**:
  - Red points and blue points form two circles.

### Problem Setup
- **Goal**: Given two numbers (`x1`, `x2`), predict whether it belongs to the red circle or the blue circle.
- This setup is **binary classification**.

## Understanding Toy Datasets
- This dataset is a **toy dataset**:
  - Small and simple enough for experimentation.
  - Still large enough to practice fundamental machine learning concepts.

## Additional Resources
- **scikit-learn Datasets**:
  - Besides circles, scikit-learn offers other toy datasets for practice.
  - These are useful for both machine learning and deep learning projects.

## Questions to Reflect On
- What are the **input** and **output shapes** of the problem?
- How would you **split** the dataset into **training** and **test** sets?

*** 72. Turning Our Data into Tensors and Making a Training and Test Split ***

what is scaler in  machine learning?
In very simple words:
A **scaler** in machine learning is something that **changes the size of your data** so that all the numbers are **more similar** to each other.

This helps the machine learn better because if some numbers are too big and others are too small, it gets confused.

**Example**:  
- Imagine you are comparing people's height (like 1.7 meters) and their salary (like 3,000,000 yen).  
- The salary numbers are so big compared to the height numbers.  
- A scaler shrinks or stretches the numbers so they are on a similar level, like both being between 0 and 1.

That's it — scaling just makes the data "even" and easier for the model to understand.

definition:
In machine learning, a **scaler** is a tool used to **transform features** (your data) so that they have a specific scale or range.

**Why scaling is important**:
- Many machine learning algorithms (especially those based on distances, like k-NN, SVM, and gradient descent-based models) work **better and faster** when features are on a similar scale.
- If one feature has very large values (e.g., income in millions) and another has very small values (e.g., height in meters), the model might give **too much importance** to the larger-scale feature.
- Scaling **helps models converge faster** and perform better.

---

### Common Types of Scalers
-------------------code----------------------
| Scaler                  | What it does                                           | Example                                      |
|--------------------------|--------------------------------------------------------|----------------------------------------------|
| **Min-Max Scaler**        | Rescales the data to a specific range, usually [0, 1].  | New value = (value - min) / (max - min)       |
| **Standard Scaler**       | Standardizes the data so that it has a mean of 0 and a standard deviation of 1. | (value - mean) / standard deviation |
| **Robust Scaler**         | Scales data using the median and interquartile range (good for data with outliers). | Based on median instead of mean |
| **MaxAbs Scaler**         | Scales each feature by its maximum absolute value (useful when data is already centered at 0). | value / max(abs(value))           |
-------------------code----------------------
---

### Quick Example
Suppose you have two features:
-------------------code----------------------
| Height (cm) | Salary (¥) |
|-------------|------------|
| 170         | 3,000,000  |
| 160         | 2,800,000  |
| 180         | 3,500,000  |
-------------------code----------------------
If you **do not scale**, "Salary" dominates because its numbers are much bigger.

After **Standard Scaling**, both features would have similar magnitudes (around mean = 0, std = 1), allowing the model to treat them **fairly**.

---

### In Code (Python using scikit-learn)
-------------------code----------------------
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
scaled_X = scaler.fit_transform(X)
-------------------code----------------------

how numbers looks like before and after scaling:
---

### Before Scaling
-------------------code----------------------
| Feature 1 (Height in cm) | Feature 2 (Salary in yen) |
|--------------------------|---------------------------|
| 160                      | 3,000,000                 |
| 170                      | 3,500,000                 |
| 180                      | 4,000,000                 |
-------------------code----------------------
You can see:  
- Heights are small numbers (around 160–180).
- Salaries are huge numbers (millions).

The model might think Salary is more important **just because** the numbers are bigger.

---

### After Scaling (for example using StandardScaler)
-------------------code----------------------
| Feature 1 (Height scaled) | Feature 2 (Salary scaled) |
|----------------------------|---------------------------|
| -1.22                      | -1.22                     |
|  0.00                      |  0.00                     |
|  1.22                      |  1.22                     |
-------------------code----------------------
Now:  
- Both height and salary are around the **same range** (around -1 to 1).
- The model treats them **fairly**.

---

**Quick visual feel:**

Before scaling:
-------------------code----------------------
Height:  160    170    180
Salary: 3000000 3500000 4000000
-------------------code----------------------

After scaling:
-------------------code----------------------
Height:  -1.22   0   1.22
Salary:  -1.22   0   1.22
-------------------code----------------------


# Lecture Notes: Preparing Data for a Neural Network in PyTorch

## 1. Recap and Current Progress
- Previously: Created classification data (blue and red dots) for practice.
- Now: Need to turn data into tensors to proceed with building a neural network.

---

## 1.1 Checking Input and Output Shapes

**Why focus on shapes?**
- Machine learning heavily uses numerical representations (tensors).
- Most common errors occur due to mismatched input/output shapes.
  
**Current Data Overview:**
- `X`: Numpy array with shape `(1000, 2)` → 1000 samples, 2 features each.
- `y`: Numpy array with shape `(1000,)` → 1000 labels (scalars, no features).

**Practical Example:**
- **Input (X):** Two numbers per sample (e.g., coordinates).
- **Output (y):** One label per sample.

**Code to view a sample:**
-------------------code----------------------
x_sample = X[0]
y_sample = y[0]

print(f"One sample of X: {x_sample}")
print(f"One sample of y: {y_sample}")

print(f"Shape of one X sample: {x_sample.shape}")
print(f"Shape of one y sample: {y_sample.shape}")  # Note: scalar, no shape
-------------------code----------------------

- **Summary:** Two features for X trying to predict one scalar label y.

---

## 1.2 Turning Data into Tensors

**Goal:** Convert Numpy arrays into PyTorch tensors and ensure proper data types.

**Steps:**
1. **Import PyTorch:**
   -------------------code----------------------
   import torch
   print(torch.__version__)
   -------------------code----------------------
   - Ensure PyTorch version is at least 1.10.

2. **Convert arrays to tensors:**
   -------------------code----------------------
   X = torch.from_numpy(X).type(torch.float)
   y = torch.from_numpy(y).type(torch.float)
   -------------------code----------------------

   - `torch.float` is equivalent to `float32`.
   - PyTorch prefers `float32` while Numpy defaults to `float64`.

3. **Check data:**
   -------------------code----------------------
   print(X[:5])
   print(y[:5])
   print(X.dtype)  # Should be torch.float32
   print(y.dtype)
   -------------------code----------------------

**Important Note:**  
- Without converting to `float32`, mismatches and errors can occur when training models.

---

## 1.3 Splitting Data into Training and Testing Sets

**Goal:** Randomly split the dataset into a training set and a testing set.

**Steps:**
1. **Import split function:**
   -------------------code----------------------
   from sklearn.model_selection import train_test_split
   -------------------code----------------------

2. **Perform split:**
   -------------------code----------------------
   X_train, X_test, y_train, y_test = train_test_split(
       X, y, 
       test_size=0.2,    # 20% test, 80% train
       random_state=42   # For reproducibility
   )
   -------------------code----------------------

3. **Check sizes:**
   -------------------code----------------------
   print(len(X_train))  # 800 samples
   print(len(X_test))   # 200 samples
   print(len(y_train))
   print(len(y_test))
   -------------------code----------------------

**Explanation:**
- `test_size=0.2` means 20% of data goes to testing, 80% to training.
- `random_state=42` ensures consistent splits across runs.

---

## Summary
- Data has been turned into PyTorch tensors.
- Data types were adjusted to match PyTorch expectations (`float32`).
- Data was split into training and testing sets.
- Next step: Build or choose a model.

*** 73. Laying Out Steps for Modelling and Setting Up Device-Agnostic Code ***

# Lecture Notes: Building a Model (Part 1)

## Recap
- In the previous lecture, we split the dataset into **training** and **test** sets using an 80/20 split.
  - **Training set**: ~800 samples
  - **Test set**: ~200 samples
- Purpose:
  - Training set: to learn patterns from the data (circles dataset - red and blue dots).
  - Test set: to evaluate how well the model has learned those patterns.
  
## Transition to Model Building
- Moving into Part 2: **Building a Model** to classify the blue and red dots.

## High-Level Steps
1. Set up **device agnostic code** (CPU or GPU).
2. **Construct the model**.
3. Define a **loss function** and **optimizer**.
4. Create **training and testing loops** (training/testing loop construction will be covered in the next section).

These steps follow the general modeling workflow:
- Pick or build a model suitable for your problem.
- Choose a loss function and optimizer.
- Build a training loop.

## Step-by-Step Breakdown

### 1. Set Up Device Agnostic Code
- **Import necessary libraries**:
  -------------------code----------------------
  import torch
  import torch.nn as nn
  -------------------code----------------------
- **Define the device**:
  -------------------code----------------------
  device = "cuda" if torch.cuda.is_available() else "cpu"
  -------------------code----------------------
  - If a GPU is available, use CUDA; otherwise, default to CPU.
  - By default, PyTorch will use the CPU unless a GPU is explicitly set up.

- **Setting up GPU (if available)**:
  - Go to `Runtime > Change Runtime Type > GPU` and save changes.
  - This restarts the runtime and reconnects the session.
  - After reconnection, rerun previous cells to redefine variables like `X_train`.

### 2. Verify Setup
- After setting the device and rerunning necessary cells, check if `X_train` and other variables are properly defined.
- Confirmed that the setup was successful.

---

## Next Step
- In the following lecture, we will move on to **constructing the model**.

*** 74. Coding a Small Neural Network to Handle Our Classification Data ***
---
# Lecture Notes: Building a Model (Part 2)

## Recap
- In the last lecture, we set up **device agnostic code**.
  - This ensures that the code runs on a **CPU** by default, but uses a **GPU** if available for faster computation.
  - This setup is essential for running the code on different hardware in the future.

## Step 2: Constructing a Model by Sub-classing `nn.Module`
- We will now build a simple model for classifying the circle dataset (red and blue dots).

### High-Level Steps
1. **Subclass `nn.Module`.**
2. **Create two `nn.Linear` layers** to handle the input data shapes.
3. **Define a `forward` method** to outline the model's forward pass.
4. **Instantiate the model** and send it to the target device.

---

## Detailed Steps

### 1. Subclass `nn.Module`
- All PyTorch models typically subclass `nn.Module` to leverage its built-in functionality.
- Create a new model class:
  -------------------code----------------------
  class CircleModelV1(nn.Module):
      def __init__(self):
          super().__init__()
          # Layers will be defined here
  -------------------code----------------------

### 2. Create Two `nn.Linear` Layers
- Define two linear layers inside the constructor:
  - **Layer 1**: input features → hidden units
  - **Layer 2**: hidden units → output feature

- Checking the input feature shape:
  - `X_train` shape: (800 samples, 2 features)
  - Therefore, input features = 2.

- Code to define the layers:
  -------------------code----------------------
  self.layer_1 = nn.Linear(in_features=2, out_features=5)
  self.layer_2 = nn.Linear(in_features=5, out_features=1)
  -------------------code----------------------

- Key points:
  - The number of hidden units (`5`) is arbitrary and for simplicity. 
  - Typically, multiples of 8 are used for efficiency (e.g., 8, 16, 32, 64).

### 3. Define the `forward` Method
- Defines how data flows through the model:
  -------------------code----------------------
  def forward(self, x):
      return self.layer_2(self.layer_1(x))
  -------------------code----------------------
- Flow:
  - Input `x` → `layer_1` → `layer_2` → Output

### 4. Instantiate the Model and Send to Target Device
- Create an instance of the model and move it to the device (CPU or GPU):
  -------------------code----------------------
  model_0 = CircleModelV1().to(device)
  -------------------code----------------------

- Verify the model is on the correct device:
  -------------------code----------------------
  next(model_0.parameters()).device
  -------------------code----------------------

- Confirmed that model parameters are on `cuda` if using a GPU.

---

## Summary
- We created a simple neural network model with two linear layers.
- The model is structured to accept two input features and output a single scalar value.
- Device agnostic coding practices were applied to allow easy switching between CPU and GPU.

---

## Next Step
- In the following lecture, we will **visualize the model structure** to better understand how the data flows through the network.

---

- how to determining the number of layers?
determining the **number of layers** in a model depends mainly on three things:

---

## 1. **The Complexity of the Problem**
- **Simple problems** (e.g., linearly separable data, small datasets) → 1–2 layers are usually enough.
- **Moderate problems** (e.g., basic image classification, simple pattern recognition) → 3–5 layers.
- **Complex problems** (e.g., image segmentation, natural language processing, video recognition) → 10+ layers, sometimes hundreds (as in ResNet-50, BERT, etc.).

**Rule:**  
> Start small. Only add more layers if your model underfits (too simple, cannot capture the data's patterns).

---

## 2. **The Size and Nature of the Data**
- **Low-dimensional data** (e.g., tabular data with few features) → shallow models (1–3 layers) often perform well.
- **High-dimensional data** (e.g., images, text, audio) → deeper models (more layers) are often needed to capture hierarchical patterns.

**Example:**  
- Tabular data → 1–2 hidden layers.
- 28x28 images (like MNIST) → 2–4 layers can work.
- Complex images (like ImageNet, 224x224x3) → often need 30–100 layers.

---

## 3. **Risk of Overfitting**
- **More layers = More parameters = Higher risk of overfitting** (memorizing the training data instead of generalizing).
- If the model overfits, you may need:
  - Regularization (dropout, weight decay)
  - More training data
  - Simpler (shallower) models

**Good Practice:**  
> Use the simplest model that achieves good performance. Adding more layers unnecessarily just makes training slower and more fragile.

---

## Practical Guidelines
| Problem Type              | Suggested Layers             |
|----------------------------|-------------------------------|
| Very simple classification | 1–2 layers                   |
| Basic image classification | 2–4 layers                   |
| Moderate complexity        | 5–10 layers                  |
| High complexity (vision/NLP) | 10+ layers (sometimes 100+)  |

---

## Example
For your **red and blue circles** dataset:
- It’s a **simple** classification task (2D data, small patterns).
- **2 layers** (input → hidden → output) is more than enough.
- You could even solve it with **one layer** sometimes.

---

## Bonus: Hidden Layer Size
When you choose how many **neurons** (units) each layer should have:
- Hidden layers often have more units than the input and output layers.
- Common hidden sizes: 8, 16, 32, 64, 128, 256, etc.
- It depends on how much *capacity* you want your model to have.

---

### Quick Checklist for Layer Decisions
- Start with 1–2 hidden layers.
- If the model underfits (cannot learn well): **add more neurons** or **add another layer**.
- If the model overfits (memorizes training data): **reduce layers** or **apply regularization**.

---

so the layer number is not depends on the data shape example of X_train.shape is 2 so we have to write two layers if 3 we have to write three layers

**the number of layers does *not* directly depend on the input data shape** like that.  
**Important:**  
> The **input shape** decides **the *size* of the input layer**, not how many *layers* you need.

---

### To be clear:
- If your `X_train.shape[1] == 2` → Your **input layer** must accept **2 features** (input size = 2).
- If your `X_train.shape[1] == 3` → Your **input layer** must accept **3 features** (input size = 3).
- **BUT** the number of **hidden layers** (middle layers) and their sizes is your design choice based on task complexity, not on the shape.

---

### Simple example
Suppose you have:
-------------------code----------------------
X_train.shape = (800, 2)  # 800 samples, 2 features
-------------------code----------------------

You can build many different models:

**Model 1 (small model):**
-------------------code----------------------
Input (2 features) → Output (1 neuron)
-------------------code----------------------
- No hidden layers.

**Model 2 (a little bigger):**
-------------------code----------------------
Input (2 features) → Hidden layer (5 neurons) → Output (1 neuron)
-------------------code----------------------
- 1 hidden layer.

**Model 3 (even bigger):**
-------------------code----------------------
Input (2 features) → Hidden1 (5 neurons) → Hidden2 (3 neurons) → Output (1 neuron)
-------------------code----------------------
- 2 hidden layers.

The *only thing* that must match is the **input size** of the first layer = the number of features in your `X_train`.

---

### Important Concepts
| Concept                         | Meaning                                                            |
|----------------------------------|--------------------------------------------------------------------|
| **Input size**                  | Must match `X_train.shape[1]` (number of features).                |
| **Number of layers**             | Depends on problem difficulty, not input size.                   |
| **Number of neurons per layer**  | You choose. Larger = more capacity, but can overfit if too large.  |

---

### Why is this?
Because **deep learning** is about learning **complex mappings**, not just matching shapes.

If the number of layers = number of features, then all deep networks would just copy the input shape, but that's not the goal. We want **layers** to **transform** the data step by step into something useful for prediction.

---

**Summary:**
- `X_train.shape[1]` decides the **input size**, not the **number of layers**.
- **You** decide how many layers based on how complex the problem is.

---

*** 75. Making Our Neural Network Visual ***

# Lecture Notes: Building a Multi-Layer Neural Network

## 1. Introduction
- Previously, we created our first multi-layer neural network.
- While it is currently just code, this represents the core process of building machine learning models in PyTorch:
  - Create layers (as simple or complex as necessary).
  - Use these layers in a **forward pass** to perform computations.

## 2. Visualizing Neural Networks with TensorFlow Playground
- **TensorFlow Playground** (playground.tensorflow.org) allows building and training neural networks visually in a browser.
- Although TensorFlow is different from PyTorch, the fundamental concepts are the same.

## 3. Model Setup
- Example dataset: circular patterns (similar to our circles dataset).
- Input features: **x1** and **x2** (two features).
- Network structure:
  - Input layer: 2 features (x1 and x2).
  - Hidden layer: 5 neurons.
  - Output layer: 1 neuron.
- Matches the model we built in code:
  - `in_features=2`, `out_features=5` for the first layer.
  - `in_features=5`, `out_features=1` for the second layer.

## 4. Key Points About the Model
- Linear activation is used for now.
- Learning rate set to 0.01.
- Binary classification task.

## 5. Understanding Loss
- **Test Loss** observed: approximately 0.5 (50%).
- Interpretation:
  - Perfect loss = 0
  - Worst loss = 1
  - A random guess in a binary classification problem (two classes) would result in about 50% loss.
- Analogy: Tossing a fair coin results in about a 50/50 distribution over many tosses.

## 6. Why the Loss Might Stay High
- Even after 3000 epochs, the loss may not improve significantly if the model's structure or settings are not ideal for the task.

## 7. Diagramming the Neural Network (with FigJam)
- Visual elements:
  - **Input nodes**: x1 and x2 (green).
  - **Hidden nodes**: Represented with blue dots.
  - **Output node**: Single output (orange).
- Arrows connecting inputs to hidden nodes and hidden nodes to output illustrate the forward pass.

## 8. Important Architectural Concepts
- Each input feature connects to every neuron in the hidden layer.
- Hidden units can be increased or adjusted to change the model’s capacity.
- The output shape of one layer must match the expected input shape of the next layer.
- Example: 
  - If an input layer outputs five values, the next layer must expect five input values.

## 9. Experimenting with TensorFlow Playground
- Suggestions:
  - Try increasing the number of hidden layers or neurons.
  - Challenge: Replicate the current network structure and try fitting it to the dataset using TensorFlow Playground.

## 10. Preview of Next Lecture
- Introduction to creating the same network with even less code.
- Upcoming focus on simplifying the network-building process.

---

*** 76. Recreating and Exploring the Insides of Our Model Using nn.Sequential ***
## **Lecture Summary: Building Neural Networks in PyTorch**

### **1. Review of Previous Work**

* Previously built a neural network model named `CircleModelV0` using PyTorch.
* Subclassed `nn.Module` to define a custom model with:

  * **Two linear layers**:

    * Input: 2 features (x)
    * Hidden: 5 features (for better learning capacity)
    * Output: 1 feature (to match y)

### **2. Visual Understanding**

* **TensorFlow Playground** was used to visualize network architecture.
* **Figma** was introduced as another tool to conceptualize neural networks.
* Visual tools help understand model architecture early in your learning journey.

### **3. Replicating the Model with `nn.Sequential`**
- our Subclassing modle
-------------------code----------------------
from sklearn import datasets

class CircleModelV0(nn.Module):
  def __init__(self):
    super().__init__()
    
    self.layer_1 = nn.Linear(in_features=2, out_features=5) 
    self.layer_2 = nn.Linear(in_features=5, out_features=1) 

  
  def forward(self, x):
    return self.layer_2(self.layer_1(x)) 
-------------------code----------------------

* Simpler alternative for straightforward models.
* Example architecture:

  -------------------code----------------------
  model = nn.Sequential(
      nn.Linear(in_features=2, out_features=5),
      nn.Linear(in_features=5, out_features=1)
  )
  -------------------code----------------------
* This approach **eliminates the need** for a `forward` method.
* Behind the scenes, `nn.Sequential` handles layer-by-layer computation.

what is nn.Sequential?
`nn.Sequential` is a class in PyTorch's `torch.nn` module that allows you to **build neural networks by stacking layers in order**, one after another.

### **Definition**

-------------------code----------------------
nn.Sequential(*args)
-------------------code----------------------

You pass layers as arguments, and `nn.Sequential` connects them end-to-end. The output of one layer becomes the input to the next.

---

### **Why Use `nn.Sequential`?**

* **Quick and simple** way to build feedforward neural networks.
* Great for models where layers flow **sequentially** without branching or custom logic.

---

### **Example**

-------------------code----------------------
import torch.nn as nn

model = nn.Sequential(
    nn.Linear(in_features=2, out_features=5),
    nn.ReLU(),
    nn.Linear(in_features=5, out_features=1)
)
-------------------code----------------------

This is equivalent to:

-------------------code----------------------
class SimpleModel(nn.Module):
    def __init__(self):
        super().__init__()
        self.layer1 = nn.Linear(2, 5)
        self.activation = nn.ReLU()
        self.layer2 = nn.Linear(5, 1)

    def forward(self, x):
        x = self.layer1(x)
        x = self.activation(x)
        x = self.layer2(x)
        return x
-------------------code----------------------

---

### **Limitations**

* You can't add **if-conditions, loops, or other logic** inside the forward pass.
* Only works for **straightforward, linear sequences** of layers.

---

Would you like a visual diagram showing how `nn.Sequential` connects the layers?


### **4. Comparison: Subclassing vs. Sequential**
-------------------code----------------------
| Method          | Pros                                           | Cons                                          |
| --------------- | ---------------------------------------------- | --------------------------------------------- |
| Subclassing     | Great for complex logic, custom forward passes | More verbose, requires manual implementation  |
| `nn.Sequential` | Simple for straightforward feedforward models  | Limited flexibility, only linear flow allowed |
-------------------code----------------------

### **5. Inspecting the Model Parameters**

* Accessed model weights and biases with `model[0].weight`, `model[0].bias`, etc.
* Each layer's parameters:

  * First layer: 2 × 5 weights + 5 biases
  * Second layer: 5 × 1 weights + 1 bias
* Parameters are **randomly initialized** unless a seed is set.
* PyTorch automates parameter creation and management.

### **6. Model Complexity**

* Increasing the number of layers and neurons increases the parameter count significantly.
* PyTorch still handles this automatically, but the underlying complexity grows.

### **7. Making Untrained Predictions**

* Predictions made on `X_test` before training:

  -------------------code----------------------
  with torch.inference_mode():
      untrained_preds = model(X_test)
  -------------------code----------------------
* Model outputs random float values due to untrained weights.
* Rounded predictions (e.g., `torch.round(preds)`) mostly result in zeroes.
* Model performs poorly before training, often around **50% accuracy** in binary tasks.

### **8. Key Concepts and Observations**

* **Model Initialization**: Starts with random weights.
* **Device Matching**: Data and model must be on the same device (e.g., CUDA).
* **Prediction Format**:

  * Labels: integers (0 or 1)
  * Predictions: floats (need post-processing for evaluation)

### **9. Next Steps**

To improve predictions:

* Introduce a **loss function**.
* Choose an **optimizer**.
* Build and implement a **training loop**.
* This will allow the model to adjust its parameters through backpropagation.


*** 77. Loss Function Optimizer and Evaluation Function for Our Classification Network ***

Here's a **clean, organized summary** of the lecture, structured in note format without emojis:

---

## Lecture Notes: Setting Up Loss Function, Optimizer, and Evaluation for Binary Classification in PyTorch

### 1. **Model Structure Recap**

* We're building a simple neural network for **binary classification**.
* Model setup:

  * Input: 2 features
  * Hidden layer: 5 units
  * Output: 1 unit (for binary classification)

---

### 2. **Loss Functions Overview**

* The loss function **measures how wrong** the model's predictions are.
* Different types are used for different problems:

  * **Regression**: `L1Loss` (Mean Absolute Error), `MSELoss` (Mean Squared Error)
  * **Classification**: `BCELoss`, `BCEWithLogitsLoss`, `CrossEntropyLoss` (for multiclass)

#### Binary Classification:

* Use: `BCEWithLogitsLoss`

  * Combines **sigmoid activation** + **binary cross entropy** in one step.
  * More **numerically stable** than applying sigmoid separately then BCELoss.

#### Reference:

* Article recommended: "Understanding Binary Cross Entropy / Log Loss" by Daniel Godoy.

---

### 3. **What is a Logit?**

* A **logit** is the raw score (real number) output **before** applying the sigmoid function.
* In deep learning, "logit" often refers to the output of the final linear layer **before activation**.
* Using `BCEWithLogitsLoss` expects logits as input and applies sigmoid internally.

---

### 4. **Sigmoid Activation Function**

* Squashes input to a value between 0 and 1.
* Formula: `1 / (1 + e^(-x))`
* Used in the **output layer** of binary classifiers to predict probabilities.

---

### 5. **Optimizers**

* Optimizers adjust model parameters to reduce loss.
* Common choices:

  * **SGD** (Stochastic Gradient Descent)
  * **Adam**
* PyTorch provides many: `torch.optim.SGD`, `Adam`, `Adagrad`, etc.

#### Example (using SGD):

-------------------code----------------------
optimizer = torch.optim.SGD(params=model.parameters(), lr=0.1)
-------------------code----------------------

---

### 6. **Evaluation Metric: Accuracy**

* Accuracy = (Number of correct predictions / Total predictions) × 100
* Useful for binary classification to measure how often the model is correct.

#### Accuracy Function:

-------------------code----------------------
def accuracy_fn(y_true, y_pred):
    correct = torch.eq(y_true, y_pred).sum().item()
    acc = (correct / len(y_pred)) * 100
    return acc
-------------------code----------------------

---

### 7. **Summary**
* We've now set up:

  * The model
  * Loss function: `BCEWithLogitsLoss` (includes sigmoid)
  * Optimizer: SGD
  * Evaluation metric: Accuracy
* Ready to move on to building the training loop in the next step.



what is binary cross entropy?

**Binary Cross-Entropy (BCE)** is a loss function used to measure how well a **binary classification** model is performing.

### Simple Explanation:

Imagine your model is trying to predict whether something is:

* **Class 0** (e.g., not a cat), or
* **Class 1** (e.g., a cat)

For each prediction, your model outputs a **probability** between 0 and 1 (like 0.8 or 0.3).
Binary Cross-Entropy compares this probability to the **true label** (which is exactly 0 or 1), and calculates **how far off** the prediction is.

---

### Formula:

If the true label is `y` and the predicted probability is `p`, then:

-------------------code----------------------
BCE = -[ y * log(p) + (1 - y) * log(1 - p) ]
-------------------code----------------------

* If the label is 1, the loss becomes `-log(p)`
* If the label is 0, the loss becomes `-log(1 - p)`

So the closer `p` is to the correct label (0 or 1), the smaller the loss.

---

### Example:

If the true label is `1` and the model predicts `0.9`:

-------------------code----------------------
Loss = -log(0.9) ≈ 0.105 (small loss → good)
-------------------code----------------------

If the model predicts `0.1`:

-------------------code----------------------
Loss = -log(0.1) ≈ 2.30 (big loss → bad)
-------------------code----------------------

---

### In PyTorch:

-------------------code----------------------
loss_fn = nn.BCELoss()
-------------------code----------------------

Or, if you're using `nn.BCEWithLogitsLoss()`, it includes the sigmoid function internally and is more stable.


what is a Logit?
A **logit** is just a **raw prediction score** (usually from a neural network) **before** it's turned into a probability.

---

### Simple Explanation:

When your model is making a prediction, it often gives you a number that could be anything — like `-3`, `0.5`, `2.7`, etc. That number is the **logit**.

To turn that number into a **probability between 0 and 1**, we pass it through the **sigmoid function**:

$$
\text{Probability} = \text{sigmoid(logit)} = \frac{1}{1 + e^{-\text{logit}}}
$$

---

### Example:

| Logit | Sigmoid Output (Probability) |
| ----- | ---------------------------- |
| -2    | 0.12                         |
| 0     | 0.50                         |
| 2     | 0.88                         |

So:

* A logit of `0` means a **50%** prediction.
* A large **positive logit** means the model is more confident in **class 1**.
* A large **negative logit** means it's more confident in **class 0**.

---

### Why Use Logits?

Using logits directly (before converting to probabilities) is more **numerically stable** when training models. That's why PyTorch provides `BCEWithLogitsLoss`, 
which expects **logits as input**, and internally applies the sigmoid.


### Imagine this:
Your model is trying to predict **yes (1)** or **no (0)**.
Before giving you the final answer as a **probability**, the model first gives a **raw score** — this score is called the **logit**.

---

### Example:

* Model gives a logit of `3` → means **strong yes**
* Model gives a logit of `-2` → means **strong no**
* Model gives a logit of `0` → means **maybe (50/50)**

Then, we use something called **sigmoid** to turn that score into a **probability** between 0 and 1.

---

### In short:

> **Logit = raw prediction score (can be any number)**
> **Sigmoid(logit) = turns it into a probability**

---


what is sigmoid activation function ?
The **sigmoid activation function** is a special math function that turns any number (positive or negative) into a value **between 0 and 1** — like a **soft yes/no** answer.

---

### Simple Explanation:

Think of sigmoid as a **squasher**:

* It takes **any number** (like -100 or +5 or 0.2)
* And **squashes** it into a range from **0 to 1**

This is useful when your model needs to **predict probabilities** — because probabilities must be between 0 and 1.

---

### Formula (you don't have to memorize this):

$$
\sigma(x) = \frac{1}{1 + e^{-x}}
$$

---

### Examples:

| Input (x) | Sigmoid(x) Output |
| --------- | ----------------- |
| -3        | 0.05              |
| 0         | 0.50              |
| 2         | 0.88              |
| 10        | 0.99995           |

So:

* Big **negative** numbers become close to **0**
* Big **positive** numbers become close to **1**
* **0** becomes **0.5** (neutral)

---

### Why use sigmoid?

* It helps your model **make decisions** like:
  “Is this a cat or not?” → Output: 0.92 (Yes)
* It's often used in the **final layer** for **binary classification**

---

*** 78. Going from Model Logits to Prediction Probabilities to Prediction Labels ***
## Lecture Notes: From Logits to Predictions and Training a Binary Classifier in PyTorch

### 1. **Loss Functions & Optimizers Recap**

* **Binary classification** requires **Binary Cross Entropy (BCE)**.
* `BCEWithLogitsLoss` is preferred due to **numerical stability** (it includes a sigmoid activation internally).
* The **optimizer** updates model parameters to **minimize loss**.
* Accuracy can be used as an additional **evaluation metric**.

---

### 2. **PyTorch Training Loop Steps**

**Remember the jingle**:

> For an epoch in a range:
> Do the forward pass → Calculate the loss → `optimizer.zero_grad()` → `loss.backward()` → `optimizer.step()`

**Detailed Steps**:

1. Forward pass
2. Calculate loss
3. Zero gradients
4. Backpropagation
5. Optimizer step (gradient descent)

---

### 3. **Understanding Logits, Probabilities, and Labels**

#### Terminology:

* **Logits**: Raw model outputs before applying any activation function.
* **Prediction probabilities**: Outputs after applying an activation (e.g., `sigmoid`).
* **Prediction labels**: Final class decisions (0 or 1) after thresholding probabilities.

#### Conversion Flow:

-------------------code----------------------
Model Output (Logits) 
→ Activation Function (Sigmoid for binary classification) 
→ Prediction Probabilities 
→ Thresholding (rounding) 
→ Prediction Labels
-------------------code----------------------

---

### 4. **Code Walkthrough: From Logits to Predictions**

-------------------code----------------------python
# Forward pass on test data (ensure it’s on the same device as the model)
y_logits = model_0(X_test.to(device))

# Convert logits to probabilities
y_pred_probs = torch.sigmoid(y_logits)

# Convert probabilities to class labels (0 or 1)
y_preds = torch.round(y_pred_probs)
-------------------code----------------------

**Why not round logits directly?**

* Rounding logits doesn’t give meaningful results.
* Sigmoid transforms logits to values in the \[0, 1] range for proper interpretation as probabilities.

---

### 5. **Decision Thresholding**

Default threshold is **0.5**:

* If `y_pred_prob ≥ 0.5` → Class **1**
* If `y_pred_prob < 0.5` → Class **0**

This can be adjusted for different sensitivity needs.

---

### 6. **Checking Predictions**

-------------------code----------------------python
# Compare predictions to expected values
torch.equal(y_preds.squeeze(), y_pred_labels)
-------------------code----------------------

* `.squeeze()` removes extra dimensions for matching shapes.
* Use `torch.inference_mode()` during evaluation to avoid tracking gradients.

---

### 7. **Next Step: Training and Testing Loop**

With all steps ready:

* Inputs: model, data, loss function (`BCEWithLogitsLoss`), optimizer
* Goal: Implement training and testing loop
* Follow the PyTorch training pattern introduced earlier

> Recommended: Try implementing the training/testing loop based on the steps before watching the next lecture.


*** 79. Coding a Training and Testing Optimization Loop for Our Classification Model ***
# Lecture Notes: Training a Binary Classification Model in PyTorch

## 1. **Introduction**

* Focus: Training a binary classification model in PyTorch.
* Previous steps covered: Model creation, data preparation, understanding logits and activation functions.
* Goal: Write a full training and evaluation loop.

---

## 2. **Understanding Logits and Activations**

* **Logits**: Raw outputs from the model’s final layer (before any activation).
* **Activation Functions**:

  * `sigmoid`: Used for **binary classification**.
  * `softmax`: Used for **multi-class classification** (not used here).
* **Converting Predictions**:

  * Logits → Probabilities: via `torch.sigmoid`
  * Probabilities → Labels: via `torch.round`
* Necessary for comparing predictions with ground-truth labels (apples-to-apples).

---

## 3. **Google Colab Notes**

* After a runtime disconnection, re-run all previous cells.
* GPU Persistence: GPU setting remains even after runtime resets.
* GPU check:

  * `device`: Verifies the device (CPU/GPU).
  * `!nvidia-smi`: Confirms presence and type of CUDA-enabled GPU (e.g., Tesla P100).

---

## 4. **Reproducibility: Setting Random Seeds**

* `torch.manual_seed(42)`
* `torch.cuda.manual_seed(42)`: For reproducibility on CUDA devices.
* Purpose: Ensure model behaves similarly across runs.

---

## 5. **Prepare Data for Training**

* Move data to the same device as the model:

  -------------------code----------------------
  X_train, y_train = X_train.to(device), y_train.to(device)
  X_test, y_test = X_test.to(device), y_test.to(device)
  -------------------code----------------------
* This makes the code device-agnostic.

---

## 6. **Training and Evaluation Loop**

### 6.1 Set Epochs

-------------------code----------------------
epochs = 100
-------------------code----------------------

### 6.2 Training Phase

-------------------code----------------------
model.train()
-------------------code----------------------

* Forward Pass:

  -------------------code----------------------
  y_logits = model(X_train).squeeze()
  y_pred = torch.round(torch.sigmoid(y_logits))
  -------------------code----------------------
* **Loss Calculation**:

  -------------------code----------------------
  loss = loss_fn(y_logits, y_train)  # BCEWithLogitsLoss expects logits
  -------------------code----------------------
* **Accuracy Calculation** (custom function):

  -------------------code----------------------
  acc = accuracy_fn(y_true=y_train, y_pred=y_pred)
  -------------------code----------------------

### 6.3 Optimizer Steps

-------------------code----------------------
optimizer.zero_grad()
loss.backward()
optimizer.step()
-------------------code----------------------

* Standard backpropagation and parameter update.

---

## 7. **Testing Phase**

-------------------code----------------------
model.eval()
with torch.inference_mode():
    test_logits = model(X_test).squeeze()
    test_pred = torch.round(torch.sigmoid(test_logits))
    test_loss = loss_fn(test_logits, y_test)
    test_acc = accuracy_fn(y_true=y_test, y_pred=test_pred)
-------------------code----------------------

---

## 8. **Loss Function Differences**

* `BCEWithLogitsLoss`: Expects **logits**.
* `BCELoss`: Expects **probabilities**. Must wrap logits with `torch.sigmoid`.

---

## 9. **Accuracy Function Parameter Order**

* Custom `accuracy_fn` follows Scikit-learn convention: `accuracy_fn(y_true, y_pred)`
* PyTorch `loss_fn`: expects `(predictions, labels)` — **order matters**.

---

## 10. **Logging Progress**

* Print every 10 epochs:

-------------------code----------------------
if epoch % 10 == 0:
    print(f"Epoch: {epoch} | "
          f"Loss: {loss:.5f}, Acc: {acc:.2f}% | "
          f"Test Loss: {test_loss:.5f}, Test Acc: {test_acc:.2f}%")
-------------------code----------------------

---

## 11. **Conclusion**

* Model is now ready for training.
* Errors are expected and encouraged for learning.
* Next step: Run the loop and start training the model.

---

*** 80. Writing Code to Download a Helper Function to Visualize Our Models Predictions ***
## **Lecture Summary: Binary Classification and Model Visualization**

### **1. Loss Functions and Output Interpretation**

* **Binary Cross-Entropy Loss (BCE Loss)**:

  * Requires **probability predictions** (0 to 1).
  * Apply `torch.sigmoid()` to model logits before passing to BCE loss.
* **BCEWithLogitsLoss**:

  * Combines sigmoid activation with BCE internally.
  * Pass **raw logits** directly—no need to apply sigmoid.
* **Custom Accuracy Function**:

  * Compares predicted labels (after thresholding) to actual labels.

### **2. Model Output Pipeline**

* Convert `logits → probabilities → predicted labels`.
* Final output should be human-readable labels (e.g., class 0 or 1).

### **3. Initial Training Attempt**

* Model trained for **100 epochs**.
* Training completed extremely fast due to:

  * **Small dataset**.
  * **Simple model**.
  * **GPU acceleration**.
* Observations:

  * Loss ≈ 0.69973 (barely changes).
  * Accuracy starts high but then decreases.
  * Model performance ≈ random guessing (around 50%).
-------------------code----------------------
Epoch: 0 | Loss: 0.69396, Accuracy: 50.88% | Test loss: 0.69261, Test acc: 51.00%
Epoch: 100 | Loss: 0.69305, Accuracy: 50.38% | Test loss: 0.69379, Test acc: 48.00%
Epoch: 200 | Loss: 0.69299, Accuracy: 51.12% | Test loss: 0.69437, Test acc: 46.00%
Epoch: 300 | Loss: 0.69298, Accuracy: 51.62% | Test loss: 0.69458, Test acc: 45.00%
Epoch: 400 | Loss: 0.69298, Accuracy: 51.12% | Test loss: 0.69465, Test acc: 46.00%
Epoch: 500 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69467, Test acc: 46.00%
Epoch: 600 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
Epoch: 700 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
Epoch: 800 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
Epoch: 900 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
-------------------code----------------------


### **4. Dataset Inspection**

* Used a dataset of **red and blue dots**.
* Class distribution:

  * **500 of each class** → **Balanced dataset**.
  * **Training set**: 400 samples/class.
  * **Test set**: 100 samples/class.
* Conclusion: Model fails to learn meaningful patterns.
-------------------code----------------------
# Check different labels
circles.label.value_counts()

label
1    500
0    500
Name: count, dtype: int64
-------------------code----------------------


### **5. Visualizing Model Predictions**

* Introduced the principle: **Visualize, visualize, visualize**.
* Strategy: Use helper function to **plot decision boundary**.
* Setup:

  * Downloaded `helper_functions.py` from the course GitHub repo using `requests` and `pathlib`.
  * Imported `plot_decision_boundary()` for visualization.
-------------------code----------------------
import requests
from pathlib import Path 

# Download helper functions from Learn PyTorch repo (if not already downloaded)
if Path("helper_functions.py").is_file():
  print("helper_functions.py already exists, skipping download")
else:
  print("Downloading helper_functions.py")
  request = requests.get("https://raw.githubusercontent.com/mrdbourke/pytorch-deep-learning/main/helper_functions.py")
  with open("helper_functions.py", "wb") as f:
    f.write(request.content)

from helper_functions import plot_predictions, plot_decision_boundary
-------------------code----------------------

### **6. Plot Interpretation**

* Training vs. test set decision boundaries:

  * Model draws a **straight line** through circular data.
  * Result: Poor fit, poor accuracy.
* Root cause: Model only uses **linear layers**.

  * Cannot capture non-linear patterns (e.g., circular clusters).
  * Linear layers draw straight-line decision boundaries.

-------------------code----------------------
# Plot decision boundaries for training and test sets
plt.figure(figsize=(12, 6))
plt.subplot(1, 2, 1)
plt.title("Train")
plot_decision_boundary(model_0, X_train, y_train)
plt.subplot(1, 2, 2)
plt.title("Test")
plot_decision_boundary(model_0, X_test, y_test)
-------------------code----------------------

### **7. Next Steps and Challenge**

* Challenge: Try training the model for **1000 epochs**.

  * Will performance improve?
* Key Question: If the model still fails, **why**?

  * Hint: Model architecture may need to be **non-linear** to fit the data properly.


*** 81. Discussing Options to Improve a Model ***
### **Review of Previous Work**

* Downloaded helper functions from `helper_functions.py`.
* Emphasis on the importance of modular code reuse:

  * Instead of rewriting, we import and reuse code.
  * Similar to how PyTorch is structured (a collection of Python scripts).
* Current goal: Train a model to separate red and blue dots.
* Existing model uses linear separation (straight lines).
* Challenge: Can the linear model ever separate this non-linear data?

---

### **Problem with Current Model**

* Model's predictions are unsatisfactory.
* Current approach isn't capturing the complexity of the data.
* Time to experiment with improvements.

---

### **Strategies to Improve the Model**

1. **Add More Layers**

   * More layers = more opportunities to learn patterns.
   * Example: Moving from a 2-layer model to a 10-layer model increases parameters, enabling better pattern recognition.
   * But too many layers can overfit or overcomplicate simple data.

2. **Increase Hidden Units**

   * More units per layer = more capacity.
   * Example: Increase from 5 to 10 hidden units per layer.
   * As with layers, adding too many might not help with simple datasets.

3. **Train for More Epochs**

   * More epochs = more passes through the data.
   * Example: Increase from 100 to 1000 epochs.
   * Challenge issued to see if this improves the linear model.

4. **Change Activation Functions**

   * Currently using **Sigmoid** (common for binary classification).
   * Introduced **ReLU** (Rectified Linear Unit) as another option.
   * Activation functions allow the network to learn non-linear patterns.

5. **Adjust Learning Rate**

   * Learning rate controls how much model weights update each epoch.
   * Too small: Training is slow, might not converge.
   * Too large: Training can diverge or lead to **exploding gradients**.
   * Also introduced concept of **vanishing gradients**.

6. **Change the Optimization Function**

   * Current: **SGD (Stochastic Gradient Descent)**.
   * Alternative: **Adam**, generally performs well across problems.

7. **Loss Function**

   * Currently using **Binary Cross Entropy** with **Sigmoid** — suitable for this binary classification task.
   * No change planned here yet.

---

### **Visual Summary (Described from Lecture Slide)**

* Compared two models:

  * **Simple Model**: Fewer layers, fewer hidden units, SGD, learning rate = 0.01, 10 epochs.
  * **Larger Model**: More layers, increased hidden units (e.g. 128 → 128 → 256), added ReLU activations, changed optimizer to Adam, reduced learning rate, increased epochs to 100.

---

### **Next Steps**

* Experiment with:

  * Adding layers.
  * Increasing hidden units.
  * Fitting for longer.
* Leave all other parameters constant for now.
* Encouraged to implement these changes and observe improvements.
* TensorBoard (a PyTorch utility for monitoring experiments) will be discussed later.
* Model saving will be done once a satisfactory model is trained.

---

*** 82. Creating a New Model with More Layers and Hidden Units ***
## **Lecture Notes: Improving the Model – Part 2 (Model Perspective)**

### **1. Recap: Model Improvements (Model Perspective)**

* Previously, improvements focused on the *model itself* rather than the *data*.
* Examples of model-focused improvements:

  * Adding more layers.
  * Increasing the number of hidden units.
  * Training (fitting) for more epochs.
  * Changing the activation function.
  * Adjusting the learning rate.
  * Changing the loss function.

---

### **2. Why "Model Perspective"?**

* These changes directly affect the model architecture or training configuration.
* They do not involve modifying the dataset.
* This contrasts with improvements from a *data perspective*, such as:

  * Increasing the number of training samples.
  * Improving data quality.

---

### **3. Parameters vs Hyperparameters**

* **Parameters**: Internal weights and biases that the model learns during training.
* **Hyperparameters**: Settings we define before training, such as:

  * Number of layers
  * Hidden units
  * Activation functions
  * Learning rate
  * Epoch count
  * Loss function

---

### **4. Experiment: Building `CircleModelV1`**

* **Goal**: Modify the model to include more hidden units, more layers, and longer training.

#### **Changes from the previous model (V0):**

* Hidden units increased from 5 to **10**.
* Number of layers increased from 2 to **3**.
* Epochs to train increased from 100 to **1000**.

> **Note**: Changing multiple hyperparameters at once makes it hard to isolate which change caused improvements or regressions.

* Best practice: Change *one hyperparameter at a time* (experiment tracking).

---

### **5. Model Implementation Details**

#### **Subclassing `nn.Module`**

* Chose to subclass `nn.Module` instead of using `nn.Sequential`:

  * More flexible and scalable.
  * Common practice in complex models.

#### **Model Definition (`CircleModelV1`)**

* `Layer 1`: Takes 2 input features → 10 hidden units.
* `Layer 2`: 10 hidden units → 10 hidden units.
* `Layer 3`: 10 hidden units → 1 output.

#### **Forward Pass**

Two styles shown:

1. **Step-by-step variable assignment using `Z`**.
2. **Nested function calls (recommended for speed)**:

-------------------code----------------------
def forward(self, x):
    return self.layer3(self.layer2(self.layer1(x)))
-------------------code----------------------

* This chaining is efficient and leverages internal PyTorch speedups.

---

### **6. Device Agnostic Model Deployment**

* Created model instance:

-------------------code----------------------
model_1 = CircleModelV1().to(device)
-------------------code----------------------

* Ensures the model is compatible with CPU or GPU.

---

### **7. Next Steps (Challenge)**

* Define loss function (likely Binary Cross Entropy as before).
* Define optimizer (e.g., SGD or Adam).
* Train the model using a training and evaluation loop.
* Try these steps independently before the next lecture.

*** 83. Writing Training and Testing Code to See if Our Upgraded Model Performs Better ***
## **Lecture Summary: Circle Model V1 - Training and Evaluation**

### **1. Recap of Progress**

* Previously, we created `CircleModelV1` by subclassing `nn.Module`.
* Enhancements over `CircleModelV0`:

  * Increased hidden units from 5 to 10.
  * Added an additional hidden layer.

### **2. Current Stage in ML Workflow**

* **Data**: Same as before.
* **Model**: New upgraded model built.
* **Next Steps**:

  * Choose loss function and optimizer.
  * Build training loop.
  * Train model, make predictions, and evaluate performance.

---
-------------------code----------------------
class CircleModelV1(nn.Module):
    def __init__(self):
        super().__init__()
        self.layer_1 = nn.Linear(in_features=2, out_features=10)
        self.layer_2 = nn.Linear(in_features=10, out_features=10) # extra layer
        self.layer_3 = nn.Linear(in_features=10, out_features=1)
        
    def forward(self, x): # note: always make sure forward is spelt correctly!
        # Creating a model like this is the same as below, though below
        # generally benefits from speedups where possible.
        # z = self.layer_1(x)
        # z = self.layer_2(z)
        # z = self.layer_3(z)
        # return z
        return self.layer_3(self.layer_2(self.layer_1(x)))

model_1 = CircleModelV1().to(device)
model_1
-------------------code----------------------

## **3. Why Add More Layers and Hidden Units?**

* More neurons/layers = more parameters = more capacity to learn complex patterns.
* Visual comparison: `model_1.state_dict()` has significantly more parameters than `model_0`.

---

## **4. Setup: Loss Function and Optimizer**

* **Loss Function**: `nn.BCEWithLogitsLoss()` for binary classification.
* **Optimizer**: `torch.optim.SGD` using `model_1.parameters()` with learning rate = `0.1`.
-------------------code----------------------
# loss_fn = nn.BCELoss() # Requires sigmoid on input
loss_fn = nn.BCEWithLogitsLoss() # Does not require sigmoid on input
optimizer = torch.optim.SGD(model_1.parameters(), lr=0.1)
-------------------code----------------------

---

## **5. Reproducibility and Training Configuration**

* Set manual seed: `torch.manual_seed(42)`, `torch.cuda.manual_seed(42)`.
* **Epochs**: 1000 (to give model ample chances to learn).
* **Three main upgrades**:

  * More hidden units.
  * More layers.
  * More training epochs.

---

## **6. Device-Agnostic Code Setup**

* Move training and test data to the appropriate device (`CPU` or `GPU`).

---

## **7. Training Loop**

* Loop over 1000 epochs:

  * **Training Phase**:

    * Set model to train mode.
    * Forward pass: `y_logits = model_1(X_train).squeeze()`.
    * Convert logits to probabilities: `torch.sigmoid(y_logits)`.
    * Convert to predicted labels: `torch.round(...)`.
    * Calculate loss and accuracy.
    * Zero gradients: `optimizer.zero_grad()`.
    * Backpropagation: `loss.backward()`.
    * Update weights: `optimizer.step()`.

  * **Evaluation Phase**:

    * Set model to evaluation mode.
    * Use `torch.inference_mode()` to disable gradient tracking.
    * Forward pass on test data.
    * Convert logits to probabilities and then predictions.
    * Compute loss and accuracy.

  * **Logging Progress**:

    * Every 100 epochs, print training and test loss/accuracy.

---
-------------------code----------------------
torch.manual_seed(42)

epochs = 1000 # Train for longer

# Put data to target device
X_train, y_train = X_train.to(device), y_train.to(device)
X_test, y_test = X_test.to(device), y_test.to(device)

for epoch in range(epochs):
    ### Training
    # 1. Forward pass
    y_logits = model_1(X_train).squeeze()
    y_pred = torch.round(torch.sigmoid(y_logits)) # logits -> prediction probabilities -> prediction labels

    # 2. Calculate loss/accuracy
    loss = loss_fn(y_logits, y_train)
    acc = accuracy_fn(y_true=y_train, 
                      y_pred=y_pred)

    # 3. Optimizer zero grad
    optimizer.zero_grad()

    # 4. Loss backwards
    loss.backward()

    # 5. Optimizer step
    optimizer.step()

    ### Testing
    model_1.eval()
    with torch.inference_mode():
        # 1. Forward pass
        test_logits = model_1(X_test).squeeze() 
        test_pred = torch.round(torch.sigmoid(test_logits))
        # 2. Caculate loss/accuracy
        test_loss = loss_fn(test_logits,
                            y_test)
        test_acc = accuracy_fn(y_true=y_test,
                               y_pred=test_pred)

    # Print out what's happening every 10 epochs
    if epoch % 100 == 0:
        print(f"Epoch: {epoch} | Loss: {loss:.5f}, Accuracy: {acc:.2f}% | Test loss: {test_loss:.5f}, Test acc: {test_acc:.2f}%")
-------------------code----------------------

## **8. Results and Analysis**

* Despite improvements, the model performs at \~50% accuracy (like random guessing).
-------------------code----------------------
Epoch: 0 | Loss: 0.69396, Accuracy: 50.88% | Test loss: 0.69261, Test acc: 51.00%
Epoch: 100 | Loss: 0.69305, Accuracy: 50.38% | Test loss: 0.69379, Test acc: 48.00%
Epoch: 200 | Loss: 0.69299, Accuracy: 51.12% | Test loss: 0.69437, Test acc: 46.00%
Epoch: 300 | Loss: 0.69298, Accuracy: 51.62% | Test loss: 0.69458, Test acc: 45.00%
Epoch: 400 | Loss: 0.69298, Accuracy: 51.12% | Test loss: 0.69465, Test acc: 46.00%
Epoch: 500 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69467, Test acc: 46.00%
Epoch: 600 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
Epoch: 700 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
Epoch: 800 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
Epoch: 900 | Loss: 0.69298, Accuracy: 51.00% | Test loss: 0.69468, Test acc: 46.00%
-------------------code----------------------

* Decision boundary plot confirms it's essentially guessing.
-------------------code----------------------
# Plot decision boundaries for training and test sets
plt.figure(figsize=(12, 6))
plt.subplot(1, 2, 1)
plt.title("Train")
plot_decision_boundary(model_1, X_train, y_train)
plt.subplot(1, 2, 2)
plt.title("Test")
plot_decision_boundary(model_1, X_test, y_test)
-------------------code----------------------
---

## **9. Hypothesis and Next Steps**

* The data is non-linear, but the model is still largely linear.
* Need to test if the model can learn anything at all.

### **Suggested Exercise**

* Use the same model on a **simple linear dataset** (e.g., from earlier in the course).
* Purpose: Confirm whether the model is functional and able to learn simpler patterns.

---

## **Conclusion**

* Although architectural complexity increased, performance remained poor due to model limitations on non-linear data.
* Visual diagnostics and testing on simpler data are crucial next steps.

---

*** 84. Creating a Straight Line Dataset to See if Our Model is Learning Anything ***
### **Lecture Summary: Preparing a Model to Learn a Straight Line (Section 5.1)**

#### **Context**

* We’ve previously attempted to build a model to separate blue and red dots (a classification task).
* So far, those efforts haven't worked well.
* The goal remains: build a **fundamental classification model** that works.

#### **Review of Previous Attempts**

* Tried training the model for more epochs.
* Added another hidden layer.
* Increased hidden units.
* Discussed ways to improve a model:

  * Tuning hyperparameters (layers, units, epochs).
  * Changing activation functions.
  * Adjusting learning rate (not done yet).
  * Modifying the loss function.

---

### **5.1: Preparing Data to Test Model with a Simpler Problem**

#### **Troubleshooting Strategy**

* **Approach**: Test the model on a simpler, known problem (fitting a straight line).
* **Why**: If the model can’t solve a simple problem, there might be a fundamental issue.

#### **Reference**

* In Section 0.1 (PyTorch Workflow Fundamentals), a simple linear model was built that worked.
* The same idea will be used here to ensure the model can learn **anything at all**.

---

### **Creating Simpler Data (Linear Regression)**

1. **Objective**: Create a dataset for a regression problem (straight line fitting).
2. **Setup**:

   * Weight = `0.7`
   * Bias = `0.3`
3. **Code Steps**:

   * Generate `x_regression` data.
   * Compute `y_regression = weight * x_regression + bias`.
   * This is a **linear regression formula without noise** (epsilon).
4. **Sanity Check**:

   * Visualize data to confirm it's valid.
   * Emphasized: *“Visualize, visualize, visualize.”*

---

### **Creating Train/Test Splits**

1. **Train/Test Split**:

   * 80% training data, 20% test data.
   * Use indexing to create `x_train_regression`, `y_train_regression`, `x_test_regression`, `y_test_regression`.

2. **Validation**:

   * Check lengths: should be 80 (train) and 20 (test) samples.
   * Long variable names are used to avoid confusion with existing data.

---

### **Using Helper Functions**

1. **Helper Functions**:

   * If not already present, download from course GitHub.
   * Contains a useful function: `plot_predictions()`.
   * Originally defined in Section 0.1.

2. **Plotting the Data**:

   * Plot training and test data to visually inspect.
   * No predictions yet, but visualization helps ensure correctness.

---

### **Thinking Ahead: Is the Model Architecture Suitable?**

* **Prompted Questions**:

  * Does our model need different input or output features for this new regression task?
  * What changes (if any) are required in `model_1` to fit this new data?

---

*** 85. Building and Training a Model to Fit on Straight Line Data ***

## **Lecture Summary: Troubleshooting Model Performance with a Simple Dataset**

### **Context and Motivation**

* We are currently working on a model that fails to fit a circular classification dataset.
* To troubleshoot, we return to a simpler problem: fitting a straight-line dataset.
* This approach helps test if the model can learn *anything* at all before addressing more complex problems.

---

### **Troubleshooting Philosophy**

* When facing a large ML problem:

  * Start with a **simpler subset** of data.
  * Use a **smaller model** before scaling up.
* In our case, we use a straight-line dataset previously built in Section 1.

---

### **Dataset Review**

* The new dataset (`x_train_regression`, `y_train_regression`) has:

  * **One input feature per sample**
  * **One output label per sample**
* Input/output shape awareness is crucial in ML.

  * Our previous model was designed for **two input features** (classification).
  * Now we need to **adjust the input features to one**.

---

### **Model 2 Setup (Adjusted for Regression)**

* Architecture:

-------------------code----------------------
model_2 = nn.Sequential(
    nn.Linear(in_features=1, out_features=10),
    nn.ReLU(),
    nn.Linear(in_features=10, out_features=1)
)
-------------------code----------------------
* This setup:

  * Takes one input feature.
  * Uses one hidden layer with 10 units.
  * Outputs one continuous value (suitable for regression).

---

### **Loss Function and Optimizer**

* Loss function: `nn.L1Loss()` (Mean Absolute Error), appropriate for regression.
* Optimizer: `torch.optim.SGD(model_2.parameters(), lr=0.1)`

  * Learning rate later tested with `0.01` for improved results.

---

### **Training Setup**

* Epochs: 1000
* Data and model sent to the appropriate device (CPU/GPU).
* Training loop:

  * Forward pass
  * Loss calculation
  * Backpropagation (`loss.backward()`)
  * Optimizer step
  * Progress printed every 100 epochs

---

### **Evaluation**

* Testing performed using `torch.inference_mode()`:

  * Predictions made on `x_test_regression`
  * Test loss computed against `y_test_regression`

---

### **Observations**

* The training and test losses **decrease**, indicating learning is happening.
* Adjusting the learning rate (e.g., to `0.01`) resulted in significantly lower test loss.

---

### **Next Steps**

* Visual confirmation:

  * Use `plot_predictions()` to check if predicted values (red dots) align closely with actual values (green dots).
* Reminder:

  * Wrap prediction in `torch.inference_mode()` for performance and correctness.

---

*** 86. Evaluating Our Models Predictions on Straight Line Data ***
## **Lecture Summary: Confirming Model Learning and Preparing for Non-Linearity**

### **Recap of Previous Steps**

* In the last session, we:

  * Solved a **simpler regression problem** to troubleshoot a complex classification model.
  * Used a **straight-line dataset** to test our model architecture (Model 2).
  * Confirmed that the model **can learn**, as evidenced by the decreasing loss.

---

### **Key Adjustments Made**

* **Input Feature Change**: Adapted input features from 2 (classification) to 1 (regression).
* **Loss Function**: Switched to `nn.L1Loss()` (Mean Absolute Error) for regression tasks.
* **Learning Rate**: Adjusted from `0.1` to `0.01` for better learning performance.
* **Device-Agnostic Code**: Ensured both model and data are on the same device (CPU or GPU).
* **Training Setup**: Reused the training loop from the classification model, now applied to regression.

---

### **Model Capacity Confirmation**

* **Why revisit a simpler dataset?**

  * To confirm that our model has *capacity to learn*.
  * If it learns well on a simple task but fails on a complex one, the problem likely lies elsewhere.

---

### **Inference and Visualization**

* Switched model to evaluation mode:

-------------------code----------------------
model_2.eval()
-------------------code----------------------
* Wrapped prediction step in inference mode:

-------------------code----------------------
with torch.inference_mode():
    y_preds = model_2(x_test_regression)
-------------------code----------------------
* Used `plot_predictions()` helper function to visualize model outputs:

  * Inputs: training/test data and labels, predicted values (`y_preds`).
  * Issue encountered: **TypeError** due to attempting `.numpy()` on CUDA tensors.
  * **Solution**: Convert tensors to CPU first:

    -------------------code----------------------
    tensor.cpu().numpy()
    -------------------code----------------------

---

### **Outcome**

* Visualization showed **red dots (predictions)** closely aligned with **green dots (actual values)**.
* This confirms that the model is learning from the regression dataset.

---

### **Next Questions**

* Why does the model fail on **non-linear classification data** (e.g., circular patterns)?
* Insight:

  * The model so far uses **linear layers only** (e.g., `nn.Linear`).
  * Linear layers are best for learning **straight-line relationships**.
  * But classification data might require **non-linear capabilities**.

---

### **Teaser for Next Lecture**

* Topic: **Non-linear activations**

  * Begin exploring `torch.nn` non-linear layers.
  * We've already used one (`nn.ReLU`)—can you identify it in our current model?
* Suggested action:

  * Review the PyTorch documentation for non-linear activation functions to get a head start.

*** 87. Introducing the Missing Piece for Our Classification Model Non-Linearity ***
## **Lecture Summary: Linear vs Non-Linear Functions in Machine Learning**

### **Recap and Context**

* The previous lecture showed our model has some learning capability—its predictions aligned well with a straight-line dataset.
* The key takeaway: **the model learned because the data was linear**, and we used **linear functions**.

---

### **Linear vs Non-Linear Functions**

* **Linear Data:** Can be represented using straight lines (`y = mx + b`).
* **Non-Linear Data:** Has curves and cannot be accurately modeled using straight lines alone.
* **Linear functions** are useful, but limited.
* **Non-linear functions** allow models to learn more complex patterns.

---

### **Machine Learning Insight**

* Neural networks are essentially a **stack of linear and non-linear functions**.
* To learn complex data distributions, we must combine these:

  * **Linear layers** identify basic patterns.
  * **Non-linear activations** allow flexibility to model curves and intricate relationships.

---

### **Real-World Analogy**

* Example: Detecting pizza in images.

  * Modeling pizza using just straight lines is impractical.
  * **Machine learning models** learn both straight and curved features to identify complex objects like pizza or sushi.
  * Writing explicit rules for every feature would be unscalable—ML models **learn these features automatically**.

---

### **Key Concept Introduced**

* **Non-Linearity** is the missing piece.

  * Without it, our model cannot learn from circular (non-linear) data.
  * It’s a **foundational concept** that recurs throughout all deep learning.

---

### **Conceptual Challenge**

> *"What patterns could you draw with an infinite combination of straight and curved lines?"*

* This emphasizes the power of deep learning: it can learn complex relationships using many linear and non-linear layers.

---

### **Preparing for Non-Linear Modeling**

* Revisiting and recreating the circular classification dataset (`make_circles`) for practice.
* **Visualization**: Red and blue circles (binary classification).

  * Improved colormap aesthetics for clearer visualization.

---

### **Data Preparation**

* Convert NumPy arrays to PyTorch tensors using `torch.from_numpy`.
* Ensure tensors are in `float` format to be compatible with PyTorch.
* Use `train_test_split` from `sklearn.model_selection` to divide data:

-------------------code----------------------
X_train, X_test, y_train, y_test = train_test_split(x, y, test_size=0.2, random_state=42)
-------------------code----------------------

---

### **Why PyTorch?**

* Supports **automatic differentiation** (via `autograd`).
* Can leverage **GPU acceleration**.
* Tensors integrate easily with numerical libraries like NumPy, but with added power.

---

### **Challenge for the Viewer**

* Explore `torch.nn` (PyTorch’s neural network module).
* Identify **non-linear activation functions**.
* Hint: One of them has already been used earlier in the notebook.

  * Try to find and understand it.

*** 88. Building Our First Neural Network with Non-Linearity ***
## **Lecture: Building a Model with Nonlinearity**

### **1. Introduction to Nonlinearity**

* **Linear = Straight lines**
* **Nonlinear = Non-straight lines**
* Nonlinearity is a key component in building powerful machine learning models.
* Linear models can only model straight-line relationships, while real-world data is often nonlinear.

### **2. Neural Network Anatomy (Recap)**

* Neural networks consist of:

  * Input layer
  * Hidden layers (can be various types: linear, pooling, padding, activation, etc.)
  * Output layer
* Hidden layers can use various PyTorch modules to introduce complexity.

---

### **3. Nonlinear Activation Functions**

* Found under `torch.nn` in PyTorch.
* Purpose: perform nonlinear transformations on input data.
* **Common examples:**

  * `Sigmoid`
  * `ReLU` (Rectified Linear Unit)

#### **ReLU Function**

* Defined as:

  * If input < 0: output = 0
  * If input ≥ 0: output = input
* This introduces nonlinearity while remaining computationally efficient.
* Visual: a piecewise function with a flat part and an increasing part.

---

### **4. Implementing a Model with Nonlinearity in PyTorch**

#### **Class Definition: `CircleModelV2`**

* Inherits from `nn.Module`
* Constructor (`__init__`) creates:

  * `layer_1`: `nn.Linear(in_features=2, out_features=10)`
  * `layer_2`: `nn.Linear(10, 10)`
  * `layer_3`: `nn.Linear(10, 1)`
  * `relu`: `nn.ReLU()`

#### **Forward Method**

-------------------code----------------------
def forward(self, x):
    return self.layer3(
        self.relu(
            self.layer2(
                self.relu(
                    self.layer1(x)
                )
            )
        )
    )
-------------------code----------------------

* Nonlinear activation (`ReLU`) is applied between each linear layer.

---

### **5. Remarks on the Model**

* Output layer is left without a final activation, as `Sigmoid` will be applied during the training or evaluation phase.
* The model (`model_3`) is instantiated and moved to the target device.

---

### **6. Practice and Challenges**

* **Challenge 1:** Train this model on the circular dataset.
* **Challenge 2:** Try replicating this architecture using the **TensorFlow Playground**:

  * Set two hidden layers (e.g., with 5 units each)
  * Use `ReLU` as the activation
  * Learning rate: 0.1 (same as used in earlier training)
  * Observe how nonlinearity helps fit nonlinear patterns.

---

### **7. Conclusion**

* Nonlinearity is crucial for handling complex data patterns.
* Activation functions like ReLU enable neural networks to approximate non-linear relationships.
* Next steps involve training this model and evaluating its performance on circular (nonlinear) data.


*** 89. Writing Training and Testing Code for Our First Non-Linear Model  ***
## **Lecture Summary: Building and Training a Nonlinear Neural Network (PyTorch)**

### **Objective**

* Recreate a simple neural network model with:

  * Two hidden layers
  * Five neurons
  * Learning rate: **0.1**
  * Activation: **ReLU** (instead of linear)

---

### **Key Concepts**

#### **1. Importance of Non-Linearity**

* **ReLU (Rectified Linear Unit)** is used as a non-linear activation function.
* Non-linearity allows neural networks to model complex, non-linear patterns.
* Neural networks = combination of **linear** and **non-linear** functions.
* This enables them to learn patterns in both simple and complex datasets.

---

#### **2. Learning Rate Impact**

* Demonstrated visually using the TensorFlow Playground:

  * **High learning rate**: Fast convergence, may overshoot.
  * **Low learning rate**: Slow convergence, may not reach optimal.
  * Best results: A learning rate that balances speed and stability (e.g., **0.1**).

---

#### **3. Loss and Optimizer**

* Problem type: **Binary Classification** (e.g., red vs blue dots).
* Loss function: **Binary Cross Entropy with Logits (BCEWithLogitsLoss)**.
* Optimizer: **Stochastic Gradient Descent (SGD)** with `lr=0.1`.

---

#### **4. Other Binary Classification Examples**

* Email spam detection (spam or not spam)
* Credit card fraud (fraud or not fraud)
* Insurance claims (at fault or not at fault)

---

### **Model Training Steps (Using PyTorch)**

#### **Setup**

* Set random seeds (`torch.manual_seed`) for reproducibility.
* Use device-agnostic code (`cuda` or `cpu`).

#### **Training Loop (1000 epochs)**

1. **Set model to training mode**: `model.train()`
2. **Forward pass**:

   * Compute logits
   * Convert logits to predictions using `torch.sigmoid` + `torch.round`
3. **Loss calculation**: Using BCEWithLogits
4. **Accuracy calculation**: Compare predictions with true labels
5. **Zero gradients**: `optimizer.zero_grad()`
6. **Backpropagation**: `loss.backward()`
7. **Parameter update**: `optimizer.step()`

---

#### **Model Parameters Check**

* Use `model.state_dict()` to inspect trainable parameters.
* Activation functions like **ReLU** have **no parameters**.

---

### **Inference (Validation Phase)**

* Set model to evaluation mode: `model.eval()` and use `torch.inference_mode()`.
* Forward pass on **test data**
* Apply `torch.sigmoid` and `torch.round` to get predicted labels.
* Compute **test loss** and **test accuracy**.

---

#### **Troubleshooting: Shape Errors**

* Common in deep learning: Shape mismatches (especially during loss computation).
* Solution: Use `.squeeze()` to remove extra dimensions.

  * Example: Predicted output shape `[200, 1]` → `[200]` using `.squeeze()`

---

### **Conclusion**

* Training with ReLU (non-linear activation) improves performance.
* Model now shows a **decrease in loss** and **increase in accuracy**.
* Reinforces the power of non-linear modeling.
* Next step: Visualize the decision boundary to confirm model learning.

*** 90. Making Predictions with and Evaluating Our First Non-Linear Model ***
### **Lecture Summary: Model Evaluation and Non-Linearity**

#### **1. Recap: First Model Training**

* The model combines **linear and non-linear functions**.
* Dataset: 500 red dots and 500 blue dots → **balanced binary classification**.
* After 1000 epochs, the model performs better than random guessing (50%).

#### **2. Importance of Non-Linearity**

* Linear models alone do not fit complex patterns well.
* Adding non-linear activation functions (e.g., ReLU, sigmoid) enables better performance.
* In **TensorFlow Playground**, non-linearity helps the model to start fitting the data.

#### **3. Transition to Coding**

* TensorFlow Playground is retired; coding resumes.
* Next focus: **Model evaluation** and **visualization**.

---

### **Model Evaluation**

#### **Setup**

* Using `model_3` (which includes non-linear functions).
* Switch to **inference mode** (`model.eval()` and `with torch.inference_mode()`).
* Predict with:

-------------------code----------------------
preds = torch.round(torch.sigmoid(model_3(X_test))).squeeze()
-------------------code----------------------

#### **Troubleshooting**

* Squeeze used to avoid shape errors — a common issue in deep learning.
* Compare predictions (`preds`) with ground truth (`y_test`).
* Confirm both are in the same format (data type, shape, device).

---

### **Visualization**

* Use a **decision boundary plot** to visually assess model predictions.
* Plot both training and test data:

-------------------code----------------------
plt.figure(figsize=(12, 6))
plt.subplot(1, 2, 1)
plot_decision_boundary(model=model_1, X=X_train, y=y_train)  # No non-linearity
plt.title("Train - Model 1")

plt.subplot(1, 2, 2)
plot_decision_boundary(model=model_3, X=X_test, y=y_test)    # With non-linearity
plt.title("Test - Model 3")
-------------------code----------------------

#### **Insights**

* **Model 1** (linear only): Poor performance.
* **Model 3** (non-linear): Visibly better decision boundary.
* Non-linearity enables more expressive decision surfaces.

---

### **Challenge to Improve Model 3**

* Current accuracy: \~79%.
* Target: **Over 80% on test data**.
* Suggestions for improvement:

  * Add more hidden layers.
  * Increase hidden units.
  * Train for more epochs.
  * Use different or additional activation functions.
  * Adjust the learning rate (e.g., lower from 0.1).

---

### **Next Steps**

* Explore how non-linear activation functions work in code.
* Upcoming: Implement these activations from scratch.

---

*** 91. Replicating Non-Linear Activation Functions with Pure PyTorch ***
## **Lecture Summary: Improving Models and Replicating Activation Functions in PyTorch**

### **1. Model Improvement Strategy**

* Previous goal: Improve Model 3 to exceed **80% accuracy** on test data.
* Suggestions:

  * **Add more layers**
  * **Increase the number of hidden units**
  * **Lower the learning rate** (e.g., to `0.01`)
  * **Train for more epochs**
* Key principle: *Experimentation is central to data science.*

---

### **2. Replicating Nonlinear Activation Functions**

#### **General Concept**

* Neural networks **discover patterns** in data through layers of **linear and nonlinear functions**.
* Even small networks (e.g., 4–5 layers) follow this principle.
* PyTorch makes it easier and faster to work with these layers by handling complex computation internally.

#### **Creating a Tensor**

-------------------code----------------------
a = torch.arange(-10, 10, 1, dtype=torch.float32)
-------------------code----------------------

* Range: `-10` to `9`
* Data type: `torch.float32` is preferred to avoid function errors.

#### **Visualizing the Data**

* Plotting `a` results in a **straight (linear) line**.

---

### **3. ReLU Activation Function**

#### **Using Built-in Function**

-------------------code----------------------
torch.relu(a)
-------------------code----------------------

* Converts all negative values to `0`, leaves positive values unchanged.

#### **Replicating ReLU Manually**

-------------------code----------------------
def relu(x: torch.Tensor) -> torch.Tensor:
    return torch.maximum(torch.tensor(0), x)
-------------------code----------------------

* Performs same operation as `torch.relu`.

#### **Visual Comparison**

* Plotting both `torch.relu(a)` and `relu(a)` shows identical output.

---

### **4. Sigmoid Activation Function**

#### **Sigmoid Formula**

$$
\sigma(x) = \frac{1}{1 + e^{-x}}
$$

#### **Using Built-in Function**

-------------------code----------------------
torch.sigmoid(a)
-------------------code----------------------

#### **Replicating Sigmoid Manually**

-------------------code----------------------
def sigmoid(x: torch.Tensor) -> torch.Tensor:
    return 1 / (1 + torch.exp(-x))
-------------------code----------------------

#### **Visual Comparison**

* Plotting both `torch.sigmoid(a)` and `sigmoid(a)` gives identical sigmoid curves.

---

### **5. Takeaways**

* **You can replicate both linear and nonlinear operations** used in PyTorch.
* Neural networks = stacking **linear** and **nonlinear** functions to learn data patterns.
* While custom functions are good for learning, PyTorch’s built-in layers are:

  * **Efficient**
  * **Error-tested**
  * **Optimized for GPU**

---

### **6. Next Topic: Multiclass Classification**

* Transitioning from **binary classification** to **multiclass classification**.
* Will apply all previously learned steps in a new context.

---

*** 92. Putting It All Together (Part 1): Building a Multiclass Dataset ***
Here is a clean, organized summary of the lecture in a structured note format:

---

## **Lecture Notes: Introduction to Multiclass Classification**

### **1. Recap of Non-Linearity in Neural Networks**

* Neural networks combine **linear and non-linear functions** to find patterns in data.
* Non-linearity allows the model to draw intricate boundaries between classes.
* Previously used non-linear activations:

  * **Sigmoid**
  * **ReLU**
* These allow networks to capture more complex decision boundaries.
* PyTorch (`torch.nn`) provides many non-linear activations and layers.

### **2. Transition from Binary to Multiclass Classification**

* **Binary Classification**: Distinguishes between two classes (e.g., cat vs dog, spam vs not spam).
* **Multiclass Classification**: Involves more than two classes (e.g., cat vs dog vs chicken).
* Key differences in architecture:

  * Use **Softmax** instead of Sigmoid for output activation.
  * Use **Cross Entropy Loss** instead of Binary Cross Entropy.

---

## **Section 8: Multiclass Classification – Putting It All Together**

### **3. Data Preparation**

#### Step 1: Import Libraries

* `torch`, `matplotlib.pyplot`, and `sklearn.datasets.make_blobs`
* Also imported `train_test_split` for data splitting.

#### Step 2: Set Hyperparameters

* `NUM_CLASSES = 4`
* `NUM_FEATURES = 2`
* `RANDOM_SEED = 42`
* `N_SAMPLES = 1000`

#### Step 3: Create Data using `make_blobs()`

* Generates isotropic Gaussian blobs for clustering:

-------------------code----------------------
X_blob, y_blob = make_blobs(
    n_samples=N_SAMPLES,
    n_features=NUM_FEATURES,
    centers=NUM_CLASSES,
    cluster_std=1.5,
    random_state=RANDOM_SEED
)
-------------------code----------------------

#### Step 4: Convert to PyTorch Tensors

* `X_blob = torch.from_numpy(X_blob).float()`
* `y_blob = torch.from_numpy(y_blob).long()`

#### Step 5: Split into Train/Test Sets

* Used `train_test_split()` from sklearn:

-------------------code----------------------
X_blob_train, X_blob_test, y_blob_train, y_blob_test = train_test_split(
    X_blob, y_blob,
    test_size=0.2,
    random_state=RANDOM_SEED
)
-------------------code----------------------

---

### **4. Data Visualization**

* Used matplotlib to visualize the blob clusters:

-------------------code----------------------
plt.figure(figsize=(10, 7))
plt.scatter(X_blob[:, 0], X_blob[:, 1], c=y_blob, cmap=plt.cm.RdYlBu)
-------------------code----------------------
* Adjusted `cluster_std` to control the spread of data points (e.g., 0 for tight clusters, 1.5 for more spread).

---

## **Next Step**

* Build the **first multiclass classification model** using PyTorch.
* The model will need to separate four different classes based on 2D input features.

---

*** 93. Creating a Multi-Class Classification Model with PyTorch ***
## Lecture Notes: Building a Multiclass Classification Model in PyTorch

### Context

* Previously created a multiclass dataset using `make_blobs`.
* Goal: Combine everything learned so far to build a **multiclass classification model**, rather than binary.

---

### Model Design Considerations

#### 1. **Input Features**

* Based on dataset: `X_train.shape` reveals 2 input features.

#### 2. **Hidden Layers**

* Hidden units are a tunable hyperparameter.
* Example: Start with `hidden_units=8`.
* Can increase based on dataset complexity or model capacity needs.

#### 3. **Output Features**

* One output per class.
* Dataset contains 4 classes → `output_features=4`.

#### 4. **Activation Function**

* Use **Softmax** at the output for multiclass classification.

#### 5. **Loss Function**

* Use `CrossEntropyLoss` (instead of `BCELoss` for binary).

#### 6. **Optimizer**

* Common choices: `SGD` or `Adam`.

---

### Device Agnostic Setup

-------------------code----------------------
device = "cuda" if torch.cuda.is_available() else "cpu"
-------------------code----------------------

* Ensures compatibility with both GPU and CPU.

---

### Model Architecture: Using `nn.Sequential`

#### Custom Model Class: `BlobModel`

-------------------code----------------------
class BlobModel(nn.Module):
    def __init__(self, input_features, out_features, hidden_units=8):
        super().__init__()
        self.linear_layer_stack = nn.Sequential(
            nn.Linear(input_features, hidden_units),
            nn.ReLU(),
            nn.Linear(hidden_units, hidden_units),
            nn.ReLU(),
            nn.Linear(hidden_units, out_features)
        )

    def forward(self, x):
        return self.linear_layer_stack(x)
-------------------code----------------------

#### Docstring (Google Style Recommended)

* Describes the class, input/output, and parameters clearly.
* Format:

  -------------------code----------------------
  """
  Initializes a multiclass classification model.

  Args:
      input_features (int): Number of input features.
      out_features (int): Number of output classes.
      hidden_units (int): Number of hidden units. Default is 8.
  """
  -------------------code----------------------

---

### Model Instantiation Example

-------------------code----------------------
model_4 = BlobModel(input_features=2, out_features=4, hidden_units=8).to(device)
-------------------code----------------------

* Make sure to match parameter names correctly (`out_features`, not `output_features`).

---

### Notes

* You can experiment by removing `ReLU()` layers to test model performance with and without non-linearity.
* Sequential models are simple and good for straightforward feedforward architectures.
* Confirm model shapes align with dataset: input features = 2, output features = 4.
* `torch.unique()` helps check how many classes exist in your dataset.

---

### What's Next?

* Define loss function and training loop.
* Continue in the next video to implement these parts.

---
