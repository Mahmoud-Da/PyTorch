*** 171. What Is Going Modular and What We Are Going to Cover ***
## **Lecture Summary: What is Going Modular?**

### **1. Purpose of the Section**

* The goal of this section is to **"go modular"**: turning reusable notebook code into **modular Python scripts**.
* Focus is on **reusability**, **organization**, and **scalability** of PyTorch projects.

---

### **2. Why Go Modular?**

* Notebooks are great for experimentation and visualization.
* However, reusable code (e.g., training loops, data loaders) should be extracted into Python scripts.
* Modular code allows:

  * Easy reuse across projects.
  * Cleaner project structure.
  * Easier collaboration and scalability.

---

### **3. Project Recap: FoodVision Mini**

* Dataset: Images of pizza, steak, and sushi.
* Tasks previously done:

  * Loaded and transformed data.
  * Built and trained a PyTorch model.
  * Made predictions (with moderate accuracy).

---

### **4. Modular Directory Structure**

By the end of this section, the project structure will look like:

-------------------code----------------------
going_modular/
│
├── going_modular/
│   ├── data_setup.py        # Handles data preparation
│   ├── engine.py            # Contains training/testing functions
│   ├── model_builder.py     # Builds PyTorch models
│   ├── train.py             # Trains a model using modular components
│   ├── utils.py             # Miscellaneous helper functions
│
├── models/                  # Stores saved models
├── data/                    # Image data in standard format
-------------------code----------------------

---

### **5. Training from the Command Line**

You will be able to train a model using a single command:

-------------------code----------------------
python train.py --model tiny_vgg --batch_size 32 --learning_rate 0.001 --num_epochs 10
-------------------code----------------------

* These are called **argument flags**.
* More hyperparameters can be added, making it customizable.
* Mimics real-world usage (e.g., Meta's [Detectron2](https://github.com/facebookresearch/detectron2), PyTorch Vision scripts).

---

### **6. Real-World Examples**

* `train.py` scripts are standard in production-level ML codebases (e.g., Meta, PyTorch).
* Common patterns:

  * Scripts accept hyperparameters via command line.
  * Modular structure improves maintainability and reproducibility.

---

### **7. Workflow Overview**

**Notebook → Python Scripts Workflow**:

1. Start in Jupyter or Google Colab for prototyping.
2. Identify reusable code.
3. Move useful cells into `.py` scripts.
4. Execute via command line with custom arguments.

---

### **8. How We'll Implement This**

Two parts of this section:

* **Part 1 – Cell Mode Notebook (`05_part1_cell_mode.ipynb`)**:

  * Standard Jupyter notebook to experiment with code.
* **Part 2 – Script Mode Notebook (`05_part2_script_mode.ipynb`)**:

  * Uses Jupyter magic commands to write code cells to `.py` files.

#### **Example Magic Command**

-------------------code----------------------
%%writefile going_modular/model_builder.py
# Contents of the cell are written to the specified file
-------------------code----------------------

---

### **9. Summary of Python Scripts**

Each component of the model pipeline is separated into its own script:

* `data_setup.py`: Prepares and loads data.
* `engine.py`: Contains training and evaluation logic.
* `model_builder.py`: Defines the PyTorch model architecture.
* `train.py`: Runs the training pipeline using the above components.
* `utils.py`: Utility functions (e.g., accuracy calculations, timer).

---

### **10. Final Goal**

* Turn useful notebook code into clean, reusable Python scripts.
* Enable training a PyTorch model from the command line with custom settings.
* Understand how this mirrors real-world machine learning development workflows.

---

*** 172. Going Modular Notebook (Part 1): Running It End to End ***
## **Lecture Summary: Going Modular with PyTorch**

### **Overview**

This lecture introduces the concept of **going modular** in PyTorch development. It is part of **Section 5** in the course and focuses on transitioning from Jupyter notebooks to modular Python scripts.

---

### **Goals of This Section**

* Familiarize with the modular approach to structuring PyTorch projects.
* Transition from **notebook-based code** to **reusable Python scripts**.
* Understand the workflow used in real-world PyTorch projects.

---

### **Resources and Structure**

* **Text-based version** of Section 5 is available in the course's eBook under `05. PyTorch Going Modular`.
* Two main notebooks:

  * **Part 1: Cell Mode** (used as the baseline)
  * **Part 2: Script Mode** (final modularized version)

---

### **Why Go Modular?**

* Real-world code is often written in scripts, not notebooks.
* Modular code is **more reusable**, **maintainable**, and **sharable**.
* Separates concerns (data handling, modeling, training logic, utilities).

---

### **Cell Mode (Part 1)**

* A condensed version of **notebook 04** (custom datasets).
* All code runs top-to-bottom as-is.
* Prepares the foundation for modularization.
* Includes:

  * Data downloading and setup
  * Model creation (`TinyVGG`)
  * Device agnostic code
  * Training and testing steps
  * Google-style docstrings added for better documentation
  * Saving the trained model to a directory

**Key Functions:**

* `train_step`
* `test_step`
* `train`
* `save_model`

**Output Files/Folders:**

* `data/` — contains downloaded dataset
* `models/` — stores trained models

---

### **Workflow: Accessing and Running the Notebook**

1. Open the notebook from the course homepage → *Going Modular* → *Part 1 (Cell Mode)*.
2. Click **Open in Colab**.
3. Click **Copy to Drive** to save a personal version.
4. Make sure to use GPU runtime.
5. Run all cells to verify that code works end-to-end.

---

### **Script Mode (Part 2) — The Goal**

* The aim is to transform Part 1 into modular scripts:

  * `going_modular/`

    * `data_setup.py`
    * `engine.py`
    * `model_builder.py`
    * `train.py`
    * `utils.py`
* This structure mirrors how PyTorch projects are structured in practice.

---

### **Next Steps**

* Begin converting code cells from Part 1 into standalone Python files.
* Understand what code belongs in which module.
* Use Part 2 as a reference to guide the modularization.

---

*** 173. Downloading a Dataset ***
## **Lecture Summary: Going Modular - Revisiting Part 1 (Cell Mode)**

### **Overview**

This lecture continues the transition toward a **modular PyTorch codebase**, revisiting the purpose and structure of **Part 1 (Cell Mode)** and preparing to modularize its contents.

---

### **Recap of the Last Video**

* Explored the **going modular section**:

  * `05. PyTorch Going Modular` (in the ebook)
  * The **"going\_modular"** directory
* Opened and ran **Part 1: Cell Mode** notebook in **Google Colab**

  * Copied to Google Drive
  * Executed all code cells

---

### **Purpose of Cell Mode**

* Part 1 notebook is a **condensed version of code** written in Section 4 (Custom Datasets).
* Focus is on identifying **useful code cells** that can be transformed into **`.py` scripts**.
* These scripts will form the modular codebase for the project.

---

### **Resetting the Runtime and File Structure**

* Demonstrated restarting the runtime and clearing generated folders:

  * Removed `data/` and `models/` directories using `!rm -rf` shell command
* Verified that these folders are **generated by notebook code**, not manually added

---

### **Section 1: Get Data**

* Reuses code from Section 4 to:

  * Import libraries (`os`, `zipfile`)
  * Set up data folder paths
  * Download the **Pizza, Steak, Sushi** image dataset
  * Unzip it and remove the original archive

**Resulting Folder Structure:**

-------------------code----------------------
data/
 ├── train/
 │    ├── pizza/
 │    ├── steak/
 │    └── sushi/
 └── test/
      ├── pizza/
      ├── steak/
      └── sushi/
-------------------code----------------------

* Each class has its own subdirectory; folder names are used as labels.

---

### **Next Step: Prepare for Script Mode**

* Section 1 will likely become its own Python script (`data_setup.py`).
* This is proposed as **the first exercise** for this section.
* Following sections will also be modularized in the same fashion.

---

### **Preview: Script Mode (Part 2)**

* Run **Part 1 (Cell Mode)** and **Part 2 (Script Mode)** side by side to compare.
* In Part 2, each section is organized more clearly:

  * For example, Section 2 becomes **2.1: Create Datasets and Dataloaders in Script Mode**
* Encouraged to explore Part 2 to understand the structure of the final modular codebase.

---

### **Key Takeaway**

The goal is to **reuse working code** from previous notebooks and **refactor it** into clean, well-organized Python modules. This reflects how real-world deep learning projects are structured.
