*** 329. Introduction to PyTorch 2.0 ***
**Lecture Summary: Introduction to PyTorch 2.0**

**1. Overview of PyTorch 2.0 Release**

* PyTorch 2.0 officially released, with the primary improvement focused on speed.
* The key new feature is `torch.compile`, a backward-compatible function that compiles models to optimize performance.
* After creating a model, passing it to `torch.compile` returns a compiled model that offers speed improvements during training and inference, especially on newer GPUs.

**2. Performance Gains**

* PyTorch tested `torch.compile` on 163 open-source models (from Huggingface Transformers, timm, Torchvision, and TorchBench).
* Tests used mixed precision (automatic mixed precision, Float16) and Float32 training.
* Results showed an average speed increase of 43% in training on Nvidia A100 GPUs.
* Personal benchmarks showed:

  * Larger batch and data sizes benefit more from `torch.compile`.
  * The first training epoch is slower, but subsequent epochs run faster.
  * Speed improvements observed were about 10% on local RTX 40 and A100 GPUs.
* Further exploration needed with mixed precision training, transformer architectures, and longer training runs, where bigger gains are expected.

**3. Technologies Behind `torch.compile`**

* `torch.compile` integrates several components:

  * Torch Dynamo
  * Aot Autograd
  * Torch Inductor
* Two main optimization techniques it offers:

  1. **Fusion (Operator Fusion)**
  2. **Graph Capture (Graph Tracing)**

**4. Fusion (Operator Fusion)**

* Fusion combines multiple operations into fewer operations to reduce overhead.
* Modern GPUs have very high compute power; the bottleneck is often memory bandwidth (speed of moving data between CPU and GPU).
* Fusion minimizes how often intermediate results are written to and read from memory, reducing bandwidth costs.
* Analogy:

  * Without fusion: Adding ingredients one by one and blending repeatedly.
  * With fusion: Adding all ingredients first, then blending once — more efficient.

**5. Graph Capture (Graph Tracing)**

* Graph capture records the sequence of operations in advance.
* Without it, operations are interpreted and executed one-by-one, causing overhead.
* Analogy:

  * Without graph capture: Driving in a new area, thinking about every turn as you go.
  * With graph capture: Driving in your own neighborhood, knowing the route ahead of time.
* Upfront time is spent recording the graph, but later executions are faster.

**6. Additional Resources**

* For a deeper understanding of fusion, memory bandwidth, and overhead, see "Making Deep Learning Go BR from First Principles" by Horace Hare.

**7. Other Notable PyTorch 2.0 Features**

* Faster Transformers is now stable, improving transformer-style model speed.
* Universal device setup introduced via a context manager or global setting.
* Enhanced Metal Performance Shaders improve PyTorch support on Mac.
* Domain-specific library upgrades in:

  * Torchvision
  * Torchtext
  * Torchrec
  * Torchaudio
  * And others.

---


*** 330. What We Are Going to Cover and PyTorch 2 Reference Materials ***

**Lecture Summary: Overview and Setup for PyTorch 2.0 Experiments**

**1. Overview of Lecture Content**

* Introduction to new PyTorch 2.0 features, with a focus on `torch.compile`.
* Setting up a series of experiments to evaluate PyTorch 2.0 improvements, primarily in speed.
* Comparing experiment results and discussing further learning resources.
* Approach: Practical coding-oriented, described as "cooking up code," emphasizing hands-on learning rather than theoretical chemistry-like explanations.

**2. Practical Setup**

* The lecture will take place in Google Colab for easy accessibility and execution.
* All relevant materials and resources are available on the website learnpytorch.io, specifically under the PyTorch 2.0 introduction section.
* For additional help, participants can use the GitHub repository linked to the course, where discussions and questions can be posted.

**3. Accessing the Course Notebook**

* The notebook for this session is located in the “Extras” section of the course materials, titled **PyTorch Deep Learning Extras > PyTorch 2.0 Intro**.
* There is an option to open the notebook directly in Google Colab using the provided button for immediate practice.

**4. Next Steps**

* The instructor will meet learners in a new Colab notebook session to proceed with hands-on coding and experimentation.



*** 331. Getting Started with PyTorch 2 in Google Colab ***
**Lecture Summary: Setting Up PyTorch 2.0 in Google Colab**

**1. Starting a New Google Colab Notebook**

* Created a new notebook titled **"PyTorch 2 Intro"** with a video tag and a brief descriptive title.
* Provided links to important reference materials:

  * The main reference notebook.
  * The official PyTorch 2.0 release notes.

**2. PyTorch 2.0 Release Notes Highlights**

* `torch.compile` is the main new API in PyTorch 2.0. It wraps a model and returns a compiled, optimized version that runs faster under the hood.
* PyTorch 2.0 is fully backward compatible — all existing PyTorch code will still run without changes.
* The changes in PyTorch 2.0 are mostly additive, meaning new features are added but existing ones remain valid.
* This ensures seamless transition for users familiar with previous PyTorch versions.

**3. Environment Setup and Version Check**

* Recorded in April 2023; at that time, Google Colab defaulted to PyTorch 2.0 or newer.
* Verified PyTorch version in Colab as 2.0.0.
* Set the runtime hardware accelerator to GPU for better performance.
* Used Google Colab Pro subscription to access premium GPUs (such as Nvidia A100).
* Acknowledged that while Pro GPUs offer better speedups, the tutorial is still valid without Pro or with older GPUs.

**4. GPU Details and Compatibility**

* Checked GPU type with `nvidia-smi` — confirmed usage of Nvidia A100, a server-class GPU.
* PyTorch 2.0 speedups are most significant on newer GPUs like A100.
* Desktop-class GPUs (e.g., Nvidia RTX 3090) show smaller speed improvements compared to server-class GPUs.
* The default backend, Torch Inductor, currently supports:

  * CPUs
  * Nvidia Volta and Ampere architectures (including A100)
  * AMD GPUs
* Older Nvidia GPUs and other GPU types are not yet fully supported by Torch Inductor.

---

*** 332. PyTorch 2.0 - 30 Second Intro ***
**Lecture Summary: Quick Overview of PyTorch 2.0 Core Upgrade**

**1. Recap on Hardware and Speedups**

* PyTorch 2.0 speed improvements are most notable on newer, server-class GPUs such as Nvidia A100.
* If using older or less powerful GPUs (e.g., without Google Colab Pro), the speedups may be less significant, but the code will still run fine.

**2. Backward Compatibility and Familiarity**

* PyTorch 2.0 is fully backward compatible with PyTorch 1.x versions.
* Existing PyTorch 1.x code, including model definitions and training routines, continue to work as before.

**3. Main New Feature: `torch.compile`**

* Example:

  * Define a model as usual, e.g., `model = torchvision.models.resnet50()`
  * Wrap the model with `torch.compile(model)` to get an optimized compiled version.
* The compiled model is a normal PyTorch model enhanced with optimizations that improve speed.
* Training and testing code remains unchanged; the compilation step simply replaces the original model with the compiled one.

**4. Compatibility with Models**

* PyTorch 2.0 aims to support *all* models, meaning `torch.compile` should work with virtually any PyTorch model.
* This broad compatibility is a key design goal.

**5. Current Caveats and Limitations**

* Some features, such as model exporting after compilation, currently have limitations.
* Exporting compiled models for deployment might not work on all devices yet.
* The PyTorch development team is aware and plans to address these issues in future releases.
* Users should check the latest PyTorch 2.0 blog and release notes for updates regarding these caveats.

---

*** 333. Getting Setup for PyTorch 2 ***
**Lecture Summary: PyTorch 2.0 Speedups and Environment Verification**

**1. Recap on Torch Compile Speedups**

* Torch Compile API is the main new addition in PyTorch 2.0, providing significant model speedups.
* Average speed improvements reported:

  * 38% for Torch Image Models (TIM library)
  * 76% for TorchBench models
  * 52% for Huggingface Transformer models
* These speedups are achieved mainly through two techniques:

  * Operator Fusion (fusion of multiple operations to reduce overhead)
  * Graph Capture (recording computation graphs for faster execution)

**2. Verifying PyTorch 2.0 Installation**

* If working locally, users may need to upgrade PyTorch to version 2.0 to access new features.
* Google Colab includes PyTorch 2.0 by default.
* Provided code snippet to check PyTorch version and install/upgrade if necessary.
* The code also outputs helpful system information for verification.

**3. Next Steps: Gathering GPU Information**

* Upcoming step is to gather detailed GPU characteristics.
* This is important because PyTorch 2.0’s main speedups (especially via `torch.compile`) are optimized for newer NVIDIA GPUs.
* Understanding GPU details helps ensure compatibility and performance expectations.

---

*** 334. Getting Info from Our GPUs and Seeing if They're Capable of Using PyTorch 2 ***
## PyTorch 2.0 GPU Compatibility and Performance Notes

### 1. PyTorch 2.0 Key Feature

* **Main feature**: `torch.compile` — optimizes models via **fusion** and **graph capture**.
* Works **best** on **newer NVIDIA GPUs**.

---

### 2. What is a "Newer" NVIDIA GPU?

* Determined by **Compute Capability** score.
* **Higher score = newer and more capable GPU**.

**Examples:**

* **A100**: Compute Capability 8.0
* **H100**: Compute Capability 9.0
* **L4**: Compute Capability 8.9
* **T4**: Compute Capability 7.5
* **V100**: Compute Capability 7.0
* **RTX 4090**: Consumer GPU, high performance.
* **RTX 3090**: Consumer GPU, good but not as capable as data center GPUs.
* **Titan RTX**: 7.5

---

### 3. Minimum Recommended Compute Capability

* **8.0 or higher** → can leverage most PyTorch 2.0 speed features.
* **Below 8.0** → PyTorch 2.0 still works (backwards compatible) but speed improvements may be smaller.

---

### 4. Desktop vs Data Center GPUs

* **Desktop Class**: GPUs like RTX 3090 (consumer hardware).
* **Data Center Class**: GPUs like A100, H100 (enterprise/cloud hardware).
* **Same architecture** (e.g., Ampere) ≠ same performance.

  * Data center GPUs often have **extra hardware capabilities** for higher performance.

---

### 5. Checking GPU Compatibility

**Method:**

1. Check **NVIDIA GPU compatibility list** online.
2. Or use PyTorch's built-in function:

   -------------------code----------------------
   torch.cuda.get_device_capability()
   -------------------code----------------------

   * Returns a tuple: `(major, minor)`
   * Example: `(8, 0)` → Compute Capability 8.0

**Logic:**

* If capability ≥ 8.0 → PyTorch 2.0 speed features fully available.
* If < 8.0 → Features still work, but speed gains are smaller.

---

### 6. Caveats and Performance Notes

* Data center GPUs usually yield **greater speedups** than desktop GPUs.
* Even GPUs with the **same architecture** may differ in performance due to hardware design.
* If using cloud environments (Google Colab Pro, AWS, etc.), GPU type impacts results.

---

*** 335. Setting the Default Device in PyTorch 2 ***
## PyTorch 2.0 — Globally Setting Devices

### 1. Context Recap

* Previous lecture: Checked GPU details.
* Example GPU: **NVIDIA A100** (Compute Capability 8.0) on Google Colab Pro.
* PyTorch 2.0 speed-up features work best on GPUs with **capability ≥ 8.0** and **data center class** GPUs.
* GPUs below 8.0 still work, but with less noticeable improvements.

---

### 2. Recommended GPU Resource

* **Blog**: “Which GPU for Deep Learning” by **Tim Dettmer**.
* Covers:

  * How GPUs work.
  * Different architectures.
  * Pros and cons of various models.
  * Performance per dollar.
  * Flowchart for GPU selection.
* Regularly updated with new GPU information.

---

### 3. Traditional Device Setting

* Previously: Set device for tensors/models manually with `.to(device)`.

  -------------------code----------------------
  tensor.to(device)
  model.to(device)
  -------------------code----------------------
* This could be repetitive and tedious.

---

### 4. New PyTorch 2.0 Feature: `set_default_device`

* Introduces **context manager** and **global device setting**.
* **Benefit**: Automatically places new tensors/models on the target device without calling `.to()`.

---

#### 4.1 Using a Context Manager

-------------------code----------------------
import torch

device = "cuda" if torch.cuda.is_available() else "cpu"

with torch.device(device):
    layer = torch.nn.Linear(20, 20)
    print(layer.weight.device)  # cuda:0
    data = torch.randn(128, 20)
    print(data.device)          # cuda:0
-------------------code----------------------

* All objects created inside the `with` block are placed on the chosen device.
* Requires **PyTorch ≥ 2.0**.

---

#### 4.2 Setting a Global Default Device

-------------------code----------------------
import torch

device = "cuda" if torch.cuda.is_available() else "cpu"
torch.set_default_device(device)

layer = torch.nn.Linear(20, 20)
print(layer.weight.device)  # cuda:0

data = torch.randn(128, 20)
print(data.device)          # cuda:0
-------------------code----------------------

* All subsequent PyTorch objects (created after this call) are placed on the chosen device.
* Also requires **PyTorch ≥ 2.0**.

---

#### 4.3 Switching Back to CPU

-------------------code----------------------
torch.set_default_device("cpu")
-------------------code----------------------

* Reverts default device to CPU.
* Useful for debugging or running parts of code without GPU.

---

### 5. Advantages

* Reduces repetitive `.to()` calls.
* Improves **code readability** and **quality of life** for PyTorch users.
* Especially useful in large projects where most tensors/models share the same device.

---

### 6. Next Step

* Prepare experiments to measure **speed improvements** from `torch.compile`.

---


*** 336. Discussing the Experiments We Are Going to Run for PyTorch 2 ***
## PyTorch 2.0 — Torch Compile Speed Testing

### 1. Context Recap

* Previous lecture:

  * **Setting device globally** (`torch.set_default_device`)
  * **Setting device with context manager** (`with torch.device(...)`)
* Now focusing on **torch.compile** — a major PyTorch 2.0 feature promising speed improvements.

---

### 2. GPU Considerations

* Testing uses **NVIDIA A100** (Compute Capability 8.0) on Google Colab Pro.
* PyTorch 2.0 works on all GPUs that support previous PyTorch versions.
* **Speed improvements** are most noticeable on:

  * GPUs with **Compute Capability ≥ 8.0**
  * Data center class GPUs (e.g., A100, H100).

---

### 3. Experiment Design

**Goal:** Compare training/testing speed **with** and **without** `torch.compile`.
**Focus:** Speed only — **not** accuracy or performance metrics.

#### 3.1 Model and Dataset

* Model: **ResNet-50** (from `torchvision`)
* Dataset: **CIFAR-10** (from `torchvision`)
* Image size: **224×224** (upsampled from CIFAR-10's default smaller size)
* Batch size: **128** (adjustable if GPU memory is limited)

#### 3.2 Experiments Overview

1. **Single run** without `torch.compile`
2. **Single run** with `torch.compile`
3. **Multi-run average** without `torch.compile`

   * Repeat experiment 1 three times, run for 5 epochs each.
4. **Multi-run average** with `torch.compile`

   * Repeat experiment 2 three times, run for 5 epochs each.

**Note:**

* Large batch size and image size used to maximize GPU utilization and better observe speedups.
* If GPU memory is smaller than the A100’s **40 GB**, reduce batch size and/or image size.

---

### 4. Why This Setup?

* ResNet-50 is a convolutional neural network (CNN).
* PyTorch team benchmarks also use **A100**, **batch size 128**, and **image size 224**.
* Possible extension: test **transformer-based models** for potentially greater relative speedups.

---

### 5. Key Parameters

| Parameter          | Value     | Notes                       |
| ------------------ | --------- | --------------------------- |
| Model              | ResNet-50 | From `torchvision`          |
| Dataset            | CIFAR-10  | From `torchvision`          |
| Image Size         | 224×224   | Increased for more GPU load |
| Batch Size         | 128       | Adjust for smaller GPUs     |
| Epochs (multi-run) | 5         | Per run                     |
| Runs (multi-run)   | 3         | For averaging               |

---

### 6. GPU Memory Check

* Command to check:

  -------------------code----------------------
  nvidia-smi
  -------------------code----------------------
* Example output for A100: **40 GB** (40,960 MB).

---

### 7. Next Steps in Code

1. **Import dependencies** (boilerplate code).
2. **Create model** and **set transforms** using `torchvision` 0.14+ model & weights API.
3. Prepare experiments 1–4 for timing comparison.

---

*** 337 ***

duplicated lecture 

